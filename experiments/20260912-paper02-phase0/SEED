# 論文 02 Phase 0 — 固定した乱数 seed
#
# provenance pass (Ollama 非消費の mock) は context i ごとに seed=i を使う。
#   PROVENANCE_SEEDS = 0,1,2,3,4,5,6,7   (k_contexts=8)
#
# 解析用の seed は **存在しない**。この pilot は解析を 1 つも行わないため
# (permutation も power 計算も回さない)。上位 ADR §4.4 が凍結している
# seed=20260708 は Phase 3 の実走で使うものであり、Phase 0 では触れない。
#
# LLM の draw 自体は seed 固定されない (Ollama の sampling)。したがって
# 再実行は「同じ凍結 context で新しい draw を引き直す」であって、
# byte 一致の replay ではない (env.md 参照)。
