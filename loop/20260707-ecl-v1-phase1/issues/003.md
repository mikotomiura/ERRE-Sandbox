# Issue 003 (I4): Ollama-free replay-verify + V4a/V4b spy + V5 annotation
verify_level: recheck   # AC 直結 (reproducibility Done gate + channel annotation)

## Goal
Ollama-free replay-verify (V2/V3a/V3b) + V4a/V4b (spy on/off) + V5 annotation を
`test_ecl_v1_locomotion.py` に実装し、`experiments/20260707-ecl-v1-locomotion/` を scaffold する。

## Background
ADR §F。V4a/V4b は synthetic recorded stream (`handoff.golden_recorded_calls()`、
destination=peripatos/start=study → move_t=1 → λ 上昇) を seeded/None state で spied replay。

## Scope
- In: V2/V3 replay-verify test (seeded state)、V4a distinct>1、V4b 相異 tick≥1 + 両 checksum 一致、
  V5 parsed-action、experiments scaffold (run.sh/repro.sh/env.md/artifacts + I3 pending note)。
- Out: I3 実 artifact 生成 (別セッション)、live Ollama。
- Allowed Files: `tests/test_integration/test_ecl_v1_locomotion.py` (追記)、
  `experiments/20260707-ecl-v1-locomotion/**`。

## Acceptance Criteria (AC↔test)
- AC1 seeded replay が inner_invocations==0 で完走し checksum 決定的 (2 回同値)
  → `test_v1_replay_deterministic_seeded`
- AC2 V4a: seeded spied replay の per-tick sampling distinct 値 > 1 → `test_v1_v4a_lambda_advances`
- AC3 V4b: seeded/None 2 spied replay の per-tick sampling 6桁比較で相異 tick ≥ 1、
  かつ両 replay の ecl_trace_checksum 一致 (幾何非摂動) → `test_v1_v4b_sampling_modulated`
- AC4 V4b silent-fail 回避: `result.decisions[*].call.sampling` は seeded/None 両 replay で同値
  (spy が必要な理由を pin) → `test_v1_v4b_decisions_call_sampling_identical`
- AC5 V5: ≥1 tick で llm_status==ok ∧ plan≠None ∧ MoveMsg resolved_from=="memory_centroid"
  → `test_v1_v5_parsed_history_dependent_action`
- AC6 experiments scaffold 存在 (repro.sh が --verify を叩く形、artifacts pending) → `test_v1_experiments_scaffold`

## Test Plan
上記 6 test。V4b の AC4 が sampling-spy 必須 (Codex HIGH-1) の実証 pin。

## Stop Conditions
seeded replay が非決定 or crash → Stop → superseding hardening ADR (記録して停止)。

## Dependencies
001, 002。

## Status
DONE

## Execution Result
`tests/test_integration/test_ecl_v1_locomotion.py` に I4 追記 + `experiments/20260707-ecl-v1-locomotion/`
scaffold (run.sh/repro.sh/env.md/artifacts/.gitkeep、I3 pending note)。全 tests は committed bundle 非依存の
in-memory synthetic (record mock → replay、`golden_recorded_calls()` destination=peripatos/start=study →
move_t=1 → λ 上昇)。実測:
- AC1 replay 決定的 + checksum == capture + inner_invocations==0。
- AC2/V4a: seeded spied replay の distinct sampling > 1 (λ 前進実証)。
- AC3/V4b: seeded/None spied replay で相異 tick ≥ 1、両 ecl_trace_checksum 一致 (幾何非摂動)。
- AC4: seeded/None の result.decisions[*].call.sampling 同値 → spy 必須の silent-fail pin (Codex HIGH-1)。
- AC5/V5: memory_centroid tick ≥ 1。
- AC6: experiments scaffold 存在 + repro.sh が --verify を叩く。
test module 全 16/16 green。full pre-push CI parity 4 段 green (3399 passed、回帰ゼロ)。
