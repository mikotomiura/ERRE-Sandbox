# Issue 001 (I1): live_v1 apparatus core
verify_level: recheck   # 公開 apparatus + AC 直結 (sampling-spy/λ0 pin)

## Goal
`live_v1.py` を新規作成し、ES-3 locomotion チャネルを live 器官に活性化する apparatus を建てる
(organ 無改変)。sampling-spy replay client (Codex HIGH-1) を含む。

## Background
ADR §B/§C/§F + §変更対象。`live.py` の sibling。既存 `run_live_capture` を写経し seeded agent_state を渡す。

## Scope
- In: 定数 (λ0/N)、seeded factory、SamplingSpyChatClient、run_live_capture_v1、LIVE_V1_OBSERVABLES、
  build_live_v1_env_pins。
- Out: CLI (I2)、replay-verify/V4/V5 test (I4)、I3 live run。
- Allowed Files: `src/erre_sandbox/integration/embodied/live_v1.py`、
  `tests/test_integration/test_ecl_v1_locomotion.py` (I1 部分を新規作成)。

## Acceptance Criteria (AC↔test)
- AC1 `ECL_V1_LOCO_LAM0` は literal `0.0`、evidence を import しない
  → `test_v1_lam0_literal_pin`
- AC2 `locomotion_seeded_agent_state()` は golden と同一 (locomotion 以外) かつ
  `locomotion == LocomotionState(lam=0.0)`、golden_agent_state は locomotion=None のまま
  → `test_v1_seeded_factory`
- AC3 `SamplingSpyChatClient` は inner の chat を委譲しつつ `sampling` を捕捉、
  `.used`/`.inner_invocations`/`.is_replay` を inner 委譲 → `test_v1_sampling_spy_captures`
- AC4 `run_live_capture_v1` を mock inner で駆動すると locomotion≠None で完走、
  かつ locomotion=None (golden) で駆動した v0 相当 run と checksum 一致条件を満たす
  (flag-off byte-invariance) → `test_v1_run_capture_flag_off_byte_invariance`
- AC5 `build_live_v1_env_pins` は base_sampling/gains/alpha/lam0/decisions_sha256 を含む
  (単一 resolved_sampling でない) → `test_v1_env_pins_structure`

## Test Plan
上記 5 test を `test_ecl_v1_locomotion.py` に新規作成。mock inner chat (v0 test 流用) で Ollama-free。

## Stop Conditions
run_live_capture_v1 が organ 改変を要する場合 → Stop (ADR 違反、設計再検討)。

## Dependencies
なし (起点)。

## Status
DONE

## Execution Result
`src/erre_sandbox/integration/embodied/live_v1.py` 新規作成。ECL_V1_LOCO_LAM0=0.0 (literal pin)、
LIVE_V1_N_COGNITION_TICKS=32、locomotion_seeded_agent_state()、SamplingSpyChatClient (Codex HIGH-1、
inner 委譲 + sampling 捕捉)、run_live_capture_v1 (live.run_live_capture へ seeded state を渡す thin wrapper、
organ 改変ゼロ)、LIVE_V1_OBSERVABLES (V1-V5 + done_formula + verdict:None)、build_live_v1_env_pins
(base/gains/α/λ0 + decisions_sha256、Codex MEDIUM-3)。I1 tests 5/5 green (test-runner)。
flag-off byte-invariance を run_live_capture_v1(golden) == live.run_live_capture(golden) で実測。
