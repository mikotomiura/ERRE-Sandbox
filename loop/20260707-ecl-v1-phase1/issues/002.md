# Issue 002 (I2): v1 CLI + 強化 measurement guard
verify_level: recheck   # security 相当 (holding 非再入 guard) + 公開 CLI

## Goal
`scripts/ecl_v1_live_capture.py` (--capture/--verify) を新規作成 (v0 script 写経、v0 無改変) し、
強化 measurement guard (Codex HIGH-2) を test に実装する。

## Background
ADR §F/§G + §変更対象。--verify は seeded state (Codex MEDIUM-2)。V4a/V4b annotation は SHA 集合外 side file。

## Scope
- In: v1 CLI (--capture: OllamaChatClient lazy + mock embedding + seeded state + render;
  --verify: Ollama-free replay-verify seeded state)、V4a/V4b side-file annotation writer、強化 guard test。
- Out: I1 apparatus、I4 の V4/V5 replay 検証 test、I3 live 実走。
- Allowed Files: `scripts/ecl_v1_live_capture.py`、`tests/test_integration/test_ecl_v1_locomotion.py` (guard test 追記)。

## Acceptance Criteria (AC↔test)
- AC1 `--verify` が committed bundle を seeded state で Ollama-free replay し
  checksum + 全 artifact SHA + manifest byte 一致を返す (I4 で実 bundle 検証、ここでは CLI 構造) → I4 と共有
- AC2 強化 guard が v1 全ファイル (live_v1/script/test) を scan し measurement import/identifier 不在を確認
  → `test_v1_measurement_guard`
- AC3 guard が `from erre_sandbox import evidence` (ImportFrom module="erre_sandbox" alias=evidence) を捕捉
  → `test_v1_guard_catches_from_import` (negative fixture)
- AC4 guard が `importlib.import_module("erre_sandbox.evidence...")` 定数 string を捕捉
  → `test_v1_guard_catches_importlib` (negative fixture)
- AC5 guard が apparatus の dict-key/filename の exact banned 名 (d_loco/evaluate_verdict/Es3Verdict/
  amplitude/headroom/floor/landscape/divergence) を捕捉、legit schema 語は誤検出しない
  → `test_v1_guard_catches_banned_key` / `test_v1_guard_allows_legit_keys`

## Test Plan
guard を `_assert_no_measurement_surface_v1` helper 化し negative fixture (AST snippet) で 3 穴を実証。

## Stop Conditions
guard が自身の banned-list 定義や description 文で self-trip → scope 見直し (DA-P1-4)。

## Dependencies
001 (live_v1 を import)。

## Status
DONE

## Execution Result
`scripts/ecl_v1_live_capture.py` 新規作成 (v0 script 写経、v0 無改変)。--capture (OllamaChatClient lazy +
mock embedding + seeded state + v1 env_pins/observables overlay render)、--verify (Ollama-free replay-verify、
**seeded state** Codex MEDIUM-2)、channel_activation() が 2 spied replay で V4a/V4b を計算し
`channel_activation_annotation.json` (SHA 集合外 side file) に出力。強化 measurement guard
(`_assert_no_measurement_surface_v1`、3 helper に分割) = import/identifier (全 v1 file) +
from-import hole1 + importlib-call-arg hole2 + dict-key/filename exact hole3 (apparatus のみ、docstring 非 scan)。
guard test 5 本 (実 3 file scan + hole1/2/3 negative fixture + legit-key positive) green。scripts/ は gate 対象外
(ruff は src tests のみ) だが hygiene で format 済。I2 tests green + script import OK。
