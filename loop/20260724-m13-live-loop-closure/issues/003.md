# Issue 003: 閉包 composition root live_loop.py（knob-on cycle + driven ループ + capture）
verify_level: recheck

## Goal
閉包専用の別 composition root `integration/embodied/live_loop.py` を新設。knob-on `CognitionCycle`
（`two_phase_knob=TwoPhaseKnob()`）を構築し `WorldRuntime(cycle=)` に注入、`run_two_phase_capture` と
同じ **tick-synchronous driven ループ**を回して inbound sink を drain → `PerceptionEvent` 注入 →
`step_cognition_once` で 1 tick 進める → per-tick capture を生成する。**outbound は既存 `_consume_result`
の enqueue+broadcast に委譲し driven ループは再 inject しない**（HIGH-1）。

## Background
- outbound owner 確定（実読）: `step_cognition_once`（tick.py:1706）→`_step_one`+`_consume_result`
  （tick.py:1748-1832）が `res.envelopes` を `_enqueue_with_drop_oldest` で `_envelopes` に enqueue 済。
  broadcaster（gateway `_broadcaster`）が drain。→ driven ループが `inject_envelope` すると**二重送出**。
- driven ループ本体 = `run_two_phase_capture`（two_phase_live.py:270-296）の
  「for tick: inject_observation → `_step_one` → `_consume_result` → build_decision → physics ticks」を踏襲。
  ただし本 issue は `step_cognition_once`（public seam）で `_step_one`+`_consume_result` を一括呼ぶ。
- knob 注入点 = `CognitionCycle.__init__(two_phase_knob=)`（cycle.py:448）+ `WorldRuntime(cycle=)`（tick.py:520）。
  organ / WorldRuntime 無改変で Option A を保つ。
- MEDIUM-2: gateway serve（uvicorn）と driven ループを**同一 event loop の TaskGroup**で supervise
  （`bootstrap._supervise` 相当の sibling supervisor）。`asyncio.Queue` は active loop 内で生成。

## Scope
### In
- `live_loop.py`: knob-on cycle 構築 / `WorldRuntime` 注入 / driven tick ループ / inbound sink drain →
  `world_perturbation_to_perception` → `inject_observation` / per-tick capture 記録
  （注入 obs 列 + served LLM call + embedding call、`observation_factory` 形状で I4 が replay 可能な dataclass 列）/
  gateway app（`make_app(runtime, inbound_sink=sink)`）+ driven ループの同一 loop TaskGroup supervise。
- fidelity: `knob=None` で回すと本番 live cognition と byte-identical になる経路（organ drift pin）。
### Out
- Plane G replay golden（I4）/ W-live 到達性 witness の完成形（I6、本 issue は trace 台帳の骨格のみ）。
- real qwen3 実走（I7、spend-gate）。UI（I5）。

## Allowed Files
- 新規: `src/erre_sandbox/integration/embodied/live_loop.py`
- 新規: `tests/test_integration/test_live_loop.py`
- 触らない（**import 駆動のみ**）: organ（`loop`/`two_phase_live`/`traversal_live`/`cycle`/`handoff`/
  `two_phase`/`locomotion_sampling`/`geometry`）/ production（`bootstrap.py`/`world/tick.py`）/ 既存 golden。

## Acceptance Criteria（AC↔test）
- AC1: `live_loop` を `knob=None` で driven させると本番 live cognition 経路と cognition 結果が byte-identical
  → `test_live_loop.py::test_knob_none_fidelity_byte_identical`
- AC2: knob-on driven で 1 摂動 → 1 cognition tick → move envelope が runtime `_envelopes` に**1 通のみ**
  （driven ループは `inject_envelope` を呼ばない=二重送出無し）
  → `test_live_loop.py::test_single_move_broadcast_no_double_send`
- AC3: inbound sink drain → PerceptionEvent 注入 → `step_cognition_once` の end-to-end が発火（例外無し・exit 0）
  → `test_live_loop.py::test_driven_loop_end_to_end_fires`
- AC4: gateway serve + driven ループが同一 event loop の TaskGroup で supervise される（別 loop/thread でない）
  → `test_live_loop.py::test_same_event_loop_taskgroup_supervise`

## Test Plan
- `tests/test_integration/test_live_loop.py` 新設。LLM 面は Ollama-free の record/replay ダブル
  （`RecordReplayChatClient` / `SamplingSpyChatClient` idiom、`two_phase_live` テスト踏襲）。

## Stop Conditions
- outbound を単一 broadcast に保てず二重送出が消せない（設計誤り→エスカレート）。
- `knob=None` fidelity が byte-identical にならない（organ drift→エスカレート）。
- driven ループと gateway serve が同一 loop で supervise できない（MEDIUM-2 未達→エスカレート）。

## Dependencies
I1（写像）, I2（`InboundSink` + gateway 配線）。

## Status
queued

## Execution Result
（完了時に記入）
