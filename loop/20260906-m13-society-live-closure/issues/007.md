# Issue 007: sealed real-run ハーネス（spend-gated、**code-path のみ**・実走は別ゲート）
verify_level: recheck

## Goal
real qwen3:8b で N 体 live セッションを封印実走するための**ハーネス**を用意し、
**Ollama-free のリハーサルが緑**であることまでを land する。
**実走そのものは user の spend ratify が要る別ゲート**であり、本 issue では走らせない。

## Background
- 前例 = `scripts/m13_live_loop_capture.py`（I7）。`--capture`（Ollama-free リハーサル）/
  `--capture --real --confirm-spend`（real qwen3:8b think=False + nomic-embed-text）/
  `--verify`（Ollama-free replay）が**同一 bundle 形状・同一 verify 経路**を通るため、
  **リハーサル緑が real bundle verify の実証になる**（DA-I7-1）。
- I7 で Codex が要求した HIGH 5 をそのまま継承する:
  - spend gate を `capture()` **本体**に置く（CLI 迂回の直呼びも拒否）
  - **unpinned provenance を拒否**（uv.lock SHA 自動導出、モデル digest / Ollama version / VRAM）
  - **sealed bundle の上書きを拒否**（tune-to-pass 経路の封鎖）
  - committed annotation を **byte 検証**
  - organ は ordinal replay ゆえ verify 層に **request conformance** を置く（I6 で実装済のものを使う）
- **事前登録（実走前に凍結、実走後の変更は Stop）**: N=3（`a_kant` / `a_nietzsche` / `a_rikyu`）、
  `n_cognition_ticks=12`、摂動 36 通、seed=0、think=False。
- **settle は正当な結果。発火するまで再走しない**（I7 の binding 規律を継承）。

## Scope
### In
- `scripts/m13_society_live_capture.py` に `--capture` / `--capture --real --confirm-spend` /
  `--verify` を実装（I6 の Plane G 骨格を拡張）。
- env pins（uv.lock SHA / qwen3 digest / ollama version / VRAM / think:false / resolved sampling /
  fixed constructor fingerprint）。`society_live.build_society_live_env_pins` を**再利用**。
- observables の事前登録（frozen、`verdict` を持たない annotation のみ）。
- Done 式の明文化（L1 = 完走 + artifact、L2 = checksum / conformance / inner_invocations=0、
  L3 = cross-platform）。
- **リハーサル（Ollama-free）を実走と同一形状で回し、`--verify` が緑**であること。
### Out
- **real 実走そのもの**（user の spend ratify で別セッション）。
- 実 Godot end-to-end（MacBook 必須、G-GEAR に godot バイナリ無し）。
- measurement 再入（effect / divergence / floor / verdict）。

## Allowed Files
- 修正/新規: `scripts/m13_society_live_capture.py`
- 新規: `experiments/<YYYYMMDD>-m13-society-live/`（run.ps1 / repro.ps1 / rehearsal 出力）
- 修正: `tests/test_integration/test_m13_society_live_capture.py`
- 触らない: `src/erre_sandbox/**`（I1-I6 で確定した範囲以外）/ organ / production / 既存 golden

## Acceptance Criteria（AC↔test）
- AC1: `--capture`（Ollama-free リハーサル）が完走し、`--verify` が緑
  → `test_m13_society_live_capture.py::test_rehearsal_capture_then_verify_is_green`
- AC2: spend gate が `capture()` 本体にあり、`--confirm-spend` 無しの **直呼び**でも拒否される
  → `test_m13_society_live_capture.py::test_spend_gate_rejects_direct_call_without_ratify`
- AC3: provenance が unpinned なら拒否（モデル digest / ollama version / uv.lock SHA いずれか欠落）
  → `test_m13_society_live_capture.py::test_unpinned_provenance_is_rejected`
- AC4: 既存 sealed bundle への**上書きが拒否**される（tune-to-pass 封鎖）
  → `test_m13_society_live_capture.py::test_sealed_bundle_overwrite_is_refused`
- AC5: replay 側チャネルの `inner_invocations == 0`、request conformance が LLM / embedding 両面で一致
  → `test_m13_society_live_capture.py::test_verify_checks_conformance_and_no_llm`
- AC6: manifest に `verdict` が無い / effect・divergence・floor・score を名乗るキーが無い
  → `test_m13_society_live_capture.py::test_manifest_has_no_verdict_or_measurement_keys`
- AC7: 事前登録定数（N=3 / ticks=12 / 摂動 36 / seed=0 / think=False）が **import 時に凍結**され、
  CLI から上書きできない
  → `test_m13_society_live_capture.py::test_preregistered_constants_are_frozen`

## Test Plan
- Ollama-free。`artifacts/` 不在の checkout では skip する形にして、bundle 未生成でも CI を壊さない
  （I7 前例）。
- cross-platform（D3）は committed rehearsal bundle を **Linux CI に verify させる**経路で取り、
  **測っている範囲**（UCRT で bake した bytes が glibc replay で同一、Linux 上で再 bake したのではない）
  を honest に明記する。

## Stop Conditions
- S3: 実走後に定数 / 摂動計画 / horizon を変えたくなった（tune-to-pass = 即 Stop）
- S4: manifest に effect / divergence / floor / verdict を入れたくなった
- **spend**: user の明示 ratify が無い限り real 実走は行わない（hard STOP）
- S5: 同一 fingerprint の失敗が 3 attempt 連続

## Dependencies
I6

## Status
未着手

## Execution Result
（完了時に記入）
