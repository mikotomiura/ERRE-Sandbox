# Issue 005: Cambridge 縦スライス — xlsx から verdict まで 1 本通す
verify_level: recheck

## Goal

**1 コーパスを端から端まで通す**。xlsx → gold 二値化 → split → anchor (ARM-A/ARM-B) →
候補ラダー 8 件 → 5 つ組指標 → `decide_external()` → `results/external-audit.json` に
Cambridge の verdict が出る。**判定規則 (I-003) と統計層 (I-004) を組み合わせて 1 本通す**。

## Background

`feedback_witness_needs_mutation_testing`: negative fixture を**作るだけ**の test は
何も検査していない。判定行を消して test が実際に落ちることまでを Done に入れる。
本 issue の判定規則には**恒真化する既知の罠が 2 つある** (DA-G1-7 / DA-G1-8)。

## Scope

### In
- `scripts/paper01_external_audit.py` に:
  - Cambridge adapter — 二値化 / **SPLIT-BLOCK (primary) + SPLIT-PARITY (sensitivity) の両方** /
    anchor **ARM-A (verdict, B=200 seeded 抽出) / ARM-B (uncapped) / ARM-D (centroid 近傍 30)** 構築
  - **negative control 2 本 (design-final §4.2、機序を識別する唯一の装置)**:
    **NC-1** = 物体 `o` の項目を**別物体 `o′`** (物体リストの巡回シフトで決定論) の split0 common で採点 /
    **NC-2** = split0 の **good** を anchor にする (向きが反転するはずの positive control)
    (ARM-A = dedupe 済プールから 30 件を `ANCHOR_DRAWS`=50 回 seeded 抽出し、
    5 つ組を抽出ごとに計算して**中央値**と 5-95 パーセンタイルを出す)
  - `AdversarialItem` (frozen dataclass) を構築して **frozen `gold_good_vs_common` に流す**
  - 候補ラダー 8 件を `Candidate` として構築 (`embed_rarity` / `jaccard_rarity` /
    `tfidf_rarity` を **frozen script から import**)
  - **5 つ組指標** — `auc_full` / `auc_lao` / `auc_membership` / `potency` /
    クラス別 near-dup 率。`potency` は **`max_r sim(x,r) ≥ REF_DEDUP` の割合**
    (float 等値比較を使わない、DA-G1-15)
  - **`decide_external()`** — `PASS` / `NO_VALID_SCORER` / `INVALID_TASK_BATTERY` のみ返す
  - bootstrap CI (`CI_ALPHA` / `N_RESAMPLES`、クラス層別リサンプル)
  - **二経路 import helper** (ERRE-Sandbox layout / paper-repo layout の両方で解決)

### Out
- Ocsai adapter と ARM-C (I-004)
- fidelity pin (I-005)
- 実走の永続化一式 (I-006)

## Allowed Files
- `scripts/paper01_external_audit.py`
- `tests/test_paper01_g1/test_external_audit.py` (新規)
- `experiments/20260907-paper01-g1/results/mutation-log.md` (新規)

## Acceptance Criteria

| AC | 条件 | test |
|---|---|---|
| 005-1 | ineligible (`auc_full < floor`) の候補を崩落と数えない | `test_ineligible_candidate_is_not_counted_as_collapse` |
| 005-2 | potency=0 の候補は `audit_not_engaged` で **PASS を与えない** | `test_zero_potency_candidate_does_not_grant_pass` |
| 005-3 | eligible が全て potency=0 → `INVALID_TASK_BATTERY` (DA-G1-11) | `test_all_potency_zero_is_invalid_task_battery` |
| 005-4 | 混在 (3 崩落 + 1 生存) → `PASS` (DA-G1-10) | `test_mixed_collapse_with_survivor_is_pass` |
| 005-5 | `decide_external` が**恒真でない** (罠 1) | `test_decide_external_is_not_tautological` |
| 005-6 | C5/C6 が primary verdict を動かさない (罠 2) | `test_lexical_candidate_does_not_drive_verdict` |
| 005-7 | gold クラスが `MIN_CLASS_N` 未満 → `INVALID_TASK_BATTERY` | `test_small_class_is_invalid_task_battery` |
| 005-8 | 二経路 import が両レイアウトで解決する | `test_dual_path_import_resolves` |
| 005-9 | **ARM-A の抽出が SEED 固定で決定論** (同 seed で 2 回 → 同一) | `test_anchor_draws_are_deterministic` |
| 005-10 | verdict が **ARM-A の中央値**から出る (ARM-B は verdict を動かさない) | `test_verdict_uses_arm_a_median_only` |
| 005-11 | 抽出後に `\|R_o\| < N_R_MIN` になる物体は落として件数を記録 | `test_object_dropped_when_pool_too_small` |
| 005-12 | **NC-1** の物体ペアリングが決定論 (巡回シフト) | `test_nc1_object_pairing_is_deterministic` |
| 005-13 | **NC-2** で判別の向きが反転する | `test_nc2_reverses_discrimination_direction` |
| 005-14 | NC-1 / NC-2 は **verdict を動かさない** | `test_negative_controls_do_not_drive_verdict` |
| 005-15 | SPLIT-BLOCK と SPLIT-PARITY の**両方**が結果 JSON に出る | `test_both_split_schemes_reported` |
| 005-16 | `AdversarialItem.object` が anchor dict のキーと一致する (ずれると静かに AUC 0.5) | `test_object_key_alignment` |
| **005-MUT** | **判定 4 行を 1 つずつ削ると pytest が実際に落ちる** → 復元して `git diff` が空 | `mutation-log.md` に 4 行分の exit code |

**mutation 対象**: ① `eligible = auc_full >= AUC_FLOOR` ② `collapsed = auc_lao < AUC_FLOOR`
③ potency>0 の条件 ④ `EMBEDDING_FAMILY` への限定

## Test Plan
- **判定規則の test は encoder 非依存** — `Candidate` を **stub rarity closure** で作り
  numpy だけで書く (`tests/test_es4_diag/test_es4_scorer_diag.py` と同じ型)。
  CI は `[eval]` extras を入れないため、ここを encoder 依存にすると CI で走らない
- 実 xlsx を使う end-to-end は `eval` marker + `pytest.importorskip("sentence_transformers")`
- 負例は**必ず同じ判定関数に通す**。dataclass を構築して自分のフィールドを assert しない

## Stop Conditions
- `scripts/es4_scorer_diag.py` の改変が要ると判明 → **S4** (停止して報告)
- `evidence/` の改変が要ると判明 → **S4**
- 事前登録した条件を変えたくなった → **S2**

## Dependencies
- I-004 (判定規則と統計層が確定していること)

## Status
queued

## Execution Result
(未着手)
