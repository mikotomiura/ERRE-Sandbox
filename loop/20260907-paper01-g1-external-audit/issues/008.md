# Issue 008: 実走と実験ディレクトリ一式 (verdict 取得 = G2)
verify_level: recheck

## Goal

`bash experiments/20260907-paper01-g1/run.sh` が **exit 0** で完走し、
2 コーパスの verdict を含む `results/external-audit.json` と `metrics.json` が出る。
**同じコマンドを 2 回走らせて JSON が byte 一致する。**

## Background

`docs/experiment-tracking.md` の規約に従う。`PYTHONHASHSEED` を固定しないと jaccard 系が
bit 再現しない (論文 01 の既知の限定。`.idea/paper-01-writing-spec.md` §8.1)。
**結果が期待と違っても再走・調整をしない。honest に記録する。**

## Scope

### In
- `experiments/20260907-paper01-g1/run.sh` — `PYTHONHASHSEED="$(cat SEED)"` を export し、
  ① `--fidelity` (I-005 の C0 再現、不一致で落ちる) → ② 2 コーパスの監査 →
  ③ 2 回目実行して SHA-256 比較 → 不一致なら exit 1
- `results/external-audit.json` — corpus × arm × candidate の 5 つ組 + corpus ごとの verdict。
  **統合 verdict は作らない** (DA-G1-12)
- `metrics.json` — 主要指標のみ
- `notes.md` — 検証した仮説 / 借用した apparatus / **逸脱と気づき**。
  Limitations (participant ID 不在 / Cambridge の A2 欠測 / rater ローテーション /
  Ocsai の MOTES 来歴 / good クラスの小ささ / 物体の狭さ) を明記
- `env.md` の最終化 (uv.lock 参照 / encoder バージョン / OS)

### Out
- paper repo への持ち出し (I-007)
- 論文本文への反映 (I-008)

## Allowed Files
- `experiments/20260907-paper01-g1/**`
- `scripts/paper01_external_audit.py` (CLI 引数の追加のみ)

## Acceptance Criteria

| AC | 条件 | 検証 |
|---|---|---|
| 008-1 | `bash run.sh` が **exit 0** | exit code |
| 008-2 | corpus ごとの `verdict` が JSON に出る (**両方**) | `results/external-audit.json` |
| 008-3 | 統合 verdict フィールドが**存在しない** | `test_no_aggregate_verdict_field` |
| 008-7 | **2 corpus available** でなければ `run.sh` は G2 未達として **exit != 0** (Codex HIGH-4) | exit code |
| 008-8 | ARM-X (source-crossed) の結果が JSON に出る | `results/external-audit.json` の `arm_x` |
| 008-4 | 2 回実行で JSON byte 一致 | `run.sh` 内の SHA-256 比較 |
| 008-5 | `notes.md` に Limitations 6 項目が揃う | `test_notes_has_required_limitations` |
| 008-6 | 進捗判定は PID 生存 + log 末尾 completion marker で行う | `run.sh` に completion marker |

## Test Plan
- 006-3 / 006-5 は encoder 非依存の構造 test
- 長時間になる場合の進捗確認は **tqdm 最新行で判定しない** —
  PID 生存 + log 末尾の completion JSON で判定する (`feedback_log_tail_completion_marker`)

## Stop Conditions
- **実走後に対応付け / floor / verdict 条件を変えたくなった → S2 (即停止)**
- 期待と違う verdict が出た → **停止しない。honest に記録して次へ進む**
  (再現しないことは正当な結果。主張を apparatus 固有に縮退させる)

## Dependencies
- I-006, I-007

## Status
queued

## Execution Result
(未着手)
