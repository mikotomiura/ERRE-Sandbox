# Issue 009: 可搬性 — 論文 repo 単体で外部監査が exit 0 (= G3)
verify_level: parse

## Goal

`pwsh paper/gather.ps1` で持ち出した先 (`C:\ERRE-Papers\anchor-recognition-not-novelty`) で
**外部監査スクリプトが単体で exit 0**、かつ `pwsh paper/gather.ps1 -Verify` が **exit 0**。

## Background

DA-6 (2026-09-07 実測): apparatus を平坦化すると `from erre_sandbox...` が解決できずに死ぬ。
本 issue の新規 script は `analysis/scripts/` に `es4_scorer_diag.py` と並ぶので、
`from scripts import ...` は**解決しない**。二経路 import helper (I-003 で実装) が
実際に効くことを、持ち出し先で確認する。

## Scope

### In
- `paper/gather.ps1` に G1 成果物の `Copy-Artifact` 行を追加
  (`scripts/paper01_external_audit.py` / Cambridge 原 xlsx + NOTICE /
  `results/external-audit.json` / `results/fidelity.json` / `config.json` / `SEED`)
- `paper/gather.ps1` が `run-external-audit.sh` を **paper repo 基準で生成**
  (verbatim コピーにしない。`PYTHONHASHSEED` 固定 + `PYTHONPATH=analysis/apparatus`)
- `paper/_closure.py` の `ENTRIES["01"]` に**新規モジュール依存が出た場合のみ**
  エントリ点を追記 (間接依存は AST が辿るので手で列挙しない)
- 持ち出し先での実行確認

### Out
- paper repo の `repro.sh` **全体**の緑化 (DA-G1-9 で scope 外)
- 論文本文

## Allowed Files
- `paper/gather.ps1`
- `paper/_closure.py`

## Acceptance Criteria

| AC | 条件 | 検証 |
|---|---|---|
| 009-1 | `pwsh paper/gather.ps1` が exit 0 で G1 成果物を集約 | exit code |
| 009-2 | 持ち出し先で `bash analysis/scripts/run-external-audit.sh` が **exit 0** | exit code |
| 009-3 | `pwsh paper/gather.ps1 -Verify` が **exit 0** (原本↔コピー SHA-256 全一致) | exit code |
| 009-4 | Cambridge 原 xlsx が byte 一致でコピーされ NOTICE が同梱される | `-Verify` の突合 |
| 009-5 | `PR #TBD` が doc / PR / commit に残っていない | `grep -r "PR #TBD"` が空 |

## Test Plan
- 手動実行 + exit code。**`data-hashes.md` は記録であって検査ではない**ので `-Verify` を必ず回す
- 持ち出し先は PUBLIC repo なので、`.steering/` が混入していないことを確認する

## Stop Conditions
- 持ち出し先で import が解決しない → 二経路 helper の設計に戻る (I-003 へ差し戻し)
- Ocsai の再配布が必要になった → **停止**。DA-G1-3 は「再配布しない」で確定している

## Dependencies
- I-008

## Status
queued

## Execution Result
(未着手)
