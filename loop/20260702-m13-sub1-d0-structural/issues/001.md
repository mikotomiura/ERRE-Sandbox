# Issue 001: D0 pack structural track 実装 + verdict run
verify_level: recheck   # AC 直結・主成果ゆえ loop-watchdog が独立再実行で緑を客観確認

## Goal
FROZEN ADR (`.steering/20260701-m13-sub1-d0-pack/design-final.md` §2-§6) の structural conformance track を
`src/erre_sandbox/evidence/d0_substrate/` に house style (ES-1/ES-3) 忠実に実装し、`test_d0_*` 全緑 +
pre-push CI parity 4 段緑 + verdict run 実走 (byte-identical 再走) まで完結する。

## Background
M13 substrate arc 第一成果物 = 二層 D0 pack。本 issue = structural track (単一縦スライス、anti-collapse
gate を分断しないため 1 issue)。ES-4 の計測器循環死を回避した非循環 structural 計測の実証が目的。
最 load-bearing = **anti-ES-1-collapse gate**。**設計 FROZEN、再オープンしない**。詳細 plan =
`C:/Users/johnd/.claude/plans/deep-painting-graham.md`、原理凍結済 defer 定数 =
`.steering/20260702-m13-sub1-d0-structural/decisions.md` DA-D0S-1。

## Scope
### In
- `d0_substrate/{__init__,constants,stub,ladder,smoke,verdict_report}.py`
- `tests/test_evidence/test_d0_{constants,stub,ladder,smoke,verdict}.py`
- `scripts/d0_structural_verdict_run.py` + handoff artifact
### Out
- semantic track (fork C / LAO) / D0b-render (Godot) / divergence 検定 / D re-entry
- 凍結 apparatus (`evidence/{spdm,es2_replay,es3_locomotion,es4_actuator}`) 変更 (read-only)
- `world.zones.ZONE_PROPS` への prop 追加 (R2 を pass させる fixture 変更は禁止)

## Allowed Files
- `src/erre_sandbox/evidence/d0_substrate/**` (新規)
- `tests/test_evidence/test_d0_*.py` (新規)
- `scripts/d0_structural_verdict_run.py` (新規)
- `.steering/20260702-m13-sub1-d0-structural/**` (verdict-result / forensic / handoff 記録)

## Acceptance Criteria (AC↔test マッピング)
- AC1 定数凍結: `test_d0_constants.py` 緑 (全定数 verbatim-pin + 継承源 `is`/値一致、defer 定数 4 値 pin)
- AC2 D0a generator: `test_d0_stub.py` 緑 (geometry byte-pin vs world.zones + replay checksum byte-identical + schema/monotone-tick/in-bounds invariant + seed-as-only-freedom)
- AC3 ladder 非循環: `test_d0_ladder.py` 緑 (各 rung null で厳密 0 崩壊 + situated-function control ≤ ZERO_TOL + ablation bit-identical + **anti-collapse gate: quantize で Δ→0 / genuine 3D で Δ>floor の両 fixture**)
- AC4 D0b veto: `test_d0_smoke.py` 緑 (physics monotone/gap-free + affordance 発火決定性 + Position/MoveMsg/ZoneTransitionEvent runtime round-trip + AgentUpdateMsg schema-only round-trip)
- AC5 verdict schema: `test_d0_verdict.py` 緑 (INCONCLUSIVE_STRUCTURAL / NO_STRUCTURAL_FLOOR / STRUCTURAL_READY 3 branch 網羅、**verdict value は pin しない**)
- AC6 verdict run: `uv run python scripts/d0_structural_verdict_run.py` が exit 0、`structural_status`/`R*` 出力 + forensic JSON 生成、**再走 byte-identical**、tune ゼロ
- AC7 CI parity: `& scripts/dev/pre-push-check.ps1` 4 段全 pass (ruff format --check / ruff check / mypy src / pytest -q)

## Test Plan
`_loop-config.json.verify.test` の `test_d0_*` 5 ファイル + `pre-push-check.ps1` 4 段。verdict run は
byte-identical 再走を手動確認 (2 回実行し diff)。

## Stop Conditions
- 全 AC 緑 (Done)
- anti-collapse gate / apparatus 妥当性が FROZEN ADR と矛盾し superseding ADR 要と判明 (Stop、escalate)
- budget (max_attempts=8 / token_ceiling=2M) 到達 (Stop)
- tune-to-pass が必要になった時点で停止 (honest に NO_STRUCTURAL_FLOOR / INCONCLUSIVE を記録し、
  R* を人為的に上げない)

## Dependencies
- 先例 (read-only): `src/erre_sandbox/evidence/{spdm,es3_locomotion}/`、`src/erre_sandbox/world/`
- import 再利用: `evidence.spdm.probe` / `evidence.bootstrap_ci` / `memory.retrieval` / `memory.store` /
  `world.{zones,physics,tick}` / `schemas`
- extras-only dep を足さない (numpy/stdlib のみ、GPU/LLM 非依存)

## Status
DONE (2026-07-02、同一セッション内で実装。worktree 分離は未使用 — model 切替 Sonnet 5 で
main checkout 上に `feat/m13-sub1-d0-structural` を切り実装、Loop 監視は簡略化)。
AC1〜AC7 全 green、pre-push CI parity 4 段 pass。

## Execution Result
`src/erre_sandbox/evidence/d0_substrate/` (constants/stub/ladder/smoke/verdict_report/__init__) +
`tests/test_evidence/test_d0_{constants,stub,ladder,smoke,verdict}.py` (67 tests, 全 green) +
`scripts/d0_structural_verdict_run.py` を実装。verdict run 実走: **`structural_status=
NO_STRUCTURAL_FLOOR`、`R*=R0`** (anti-ES-1-collapse gate が R1 で floor 未達、honest tune-less
finding。詳細 = `.steering/20260702-m13-sub1-d0-structural/verdict-result.md`)。D0b-runtime smoke
PASS。handoff.json + sample_replay/ 出力済。pre-push CI parity 4 段全 pass (3229 passed, 66
skipped)。PR は TASK-POST `/cross-review` 後に作成。
