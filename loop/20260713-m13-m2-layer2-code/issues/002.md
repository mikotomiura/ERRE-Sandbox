# Issue 002 (J2): build_self_other_context pure builder（strict prefix filter・float-free）
verify_level: recheck

## Goal
`build_self_other_context(*, observer_id, window_index, prior_records) -> SelfOtherContext` を
society.py に **pure 決定関数** として実装（v1 graft）。他者 prior-window 記録行動 → canonical
float-free payload + 決定的 render text segment。**strict prefix filter**
（`other_agent_id != observer_id` ∧ `source_window < window_index`）で self/future leak を構造排除。

## Background
契約 §L3（pure builder 行）+ §L7（payload schema）+ DA-L2-2（strict prefix filter）+ Codex HIGH-2。
payload = §L7 の構造（injections[] canonical-sorted by (window, observer_agent_id)、observed[]
canonical-sorted by other_agent_id、scalar は {str,int,bool,None} のみ float 排除）。render は
payload の pure 関数、SimToM framing template（functional-analog 語彙、bounded）。
※ SimToM 最終文言 + think=False parseability desk-audit は J4 に集約（builder は render 骨格のみ、
文言 const は J4 desk-audit で確定）。ここでは builder purity + strict prefix filter を確立。

## Scope
### In
- `SelfOtherContext` 型（payload + render text を保持、frozen dataclass）。
- `build_self_other_context` pure builder（strict prefix filter、canonical sort、float-free payload）。
- observed record 由来 = Layer1 event log（zone/destination/utterance/proximity bool）のみ（規律 d）。
### Out
- slot 集約（J3 = run_society_loop が builder を呼んで run-level payload に集約）。
- continuity gate（J4）、guard（J5）。SimToM 最終文言 desk-audit（J4）。

## Allowed Files
- `src/erre_sandbox/integration/embodied/society.py`（build_self_other_context + SelfOtherContext additive）
- `tests/test_integration/test_m2_society_selfother.py`（J2 分の test 追記）

## Acceptance Criteria（AC↔test）
- **AC1** 同一 others-prior-records → 同一 canonical context（決定的・順序不感・float-free）。
  → `test_self_other_context_builder_purity`（#3）
- **AC2** 2-window fixture で builder が `window_index=t` に対し `other != observer` ∧
  `source_window < t` の prefix のみ読む = this-window/future/自己行動が context に混入したら赤くなる。
  → `test_self_other_no_future_or_self_leak`（#6）
- **AC3** payload の scalar は {str,int,bool,None} のみ（float 非在）+ list は canonical-sort 済。
  → `test_self_other_context_builder_purity` 内で payload scalar 型 + sort 順を assert。

## Test Plan
- `test_self_other_context_builder_purity`: 同一入力 2 回 → 同一 payload/render；observed の入力順を
  shuffle → 同一 canonical 出力（順序不感）；payload を walk して float 非在 + list sort 順を assert。
- `test_self_other_no_future_or_self_leak`: 2-window fixture（observer 自身 + 未来 window の record を
  混ぜる）→ window t の context に self/future record が現れないことを exact-oracle で assert。

## Stop Conditions
- payload に float が混入（cross-platform drift 構造、判断3 教訓）→ 停止。
- builder が memory/store を読む（pure でない、循環リスク）→ 停止。
- observed に AppraisalState measurement field が載る（規律 d 違反）→ 停止。

## Dependencies
- なし（pure builder は独立。J3 が集約で使う）。

## Status
DONE

## Execution Result
- `SelfOtherPriorRecord`（builder input）/ `SelfOtherObservedRecord`（observed item、float-free
  payload）/ `SelfOtherContext`（observer_agent_id/window/source_window/observed/rendered）を
  society.py に frozen dataclass 追加。`M2_SELFOTHER_SCHEMA_VERSION = "m2-selfother-1"`。
- `build_self_other_context(*, observer_id, window_index, prior_records)` pure builder: strict prefix
  filter（`agent_id != observer_id` ∧ `window == window_index-1` = source_window t-1、`< window_index`
  by construction）、observed は other_agent_id で canonical sort、window 0 → empty。store/memory/RNG/
  wall-clock を一切読まない pure seam。
- `_render_self_other` = SimToM segment（functional-analog framing const `_SELF_OTHER_FRAMING`、規律 b
  遵守、think=False parseability 用に bounded/imperative。最終 desk-audit は J4）。
- payload float-free（scalar は {str,int,bool,None} のみ、was_proximate は bool で距離 float 非搭載）。
- test: `test_self_other_context_builder_purity`（#3、purity/order-insensitive/float-free/canonical sort）+
  `test_self_other_no_future_or_self_leak`（#6、2-window fixture で self/this-window/future/t-2 排除）緑。
- mypy clean。
