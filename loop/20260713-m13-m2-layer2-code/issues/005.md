# Issue 005 (J5): spend ast-guard + functional-analog vocab guard + handoff additive bump + Layer2 golden
verify_level: recheck

## Goal
measurement 非再入の機械保証（spend ast-guard = allowlist import + Call/Attribute/field/import root 走査
+ no-measurement-computation + runtime call-count cap）。functional-analog vocabulary guard（規律 b、
禁止語なし）。handoff manifest schema additive bump（legacy/off byte unchanged / Layer2 N=1
canonical-equivalent）。Layer2 golden bake（N≥2・Layer2 on、WSL byte 一致実測）。

## Background
契約 §L9（spend ast-guard、Layer1 §M8 / B §I4 同型）+ §L7 禁止 field 名 + Codex MEDIUM-1（runtime
call-count）/ MEDIUM-4（Call/Attribute/field/import root 走査）+ §L8 handoff + §L4.3 vocabulary guard。
house pattern = test_m2_society_spend_guard.py（allowlist + no-measurement-computation + call-cap）+
test_ecl_bank_continuity.py（self-scan-aware vocab guard、`_identifier_tokens` idiom）。
handoff = 新 schema version 定数（例 `M2_SELFOTHER_MANIFEST_SCHEMA_VERSION`）、legacy/off byte unchanged。
Layer2 payload float-free ゆえ WSL byte 一致は継承 serializer + canonical-sort で構造保証。

## Scope
### In
- spend ast-guard test（Layer2 builder + selfother test の import allowlist / no-measurement-computation /
  禁止 field 名[magnitude/score/confidence/utility/appraisal_state 系] / runtime call-count on/off 同一）。
- functional-analog vocabulary guard（Layer2 src の builder + prompt template に禁止語[mirror neuron/
  神経機構再現 等]なし、self-scan-aware）。
- handoff.py に Layer2 manifest schema additive bump（`M2_SELFOTHER_MANIFEST_SCHEMA_VERSION`）。
- Layer2 golden fixture bake（`tests/fixtures/m2_society_selfother_golden/`、N≥2・Layer2 on）+
  committed golden match test + WSL byte 一致実測。
### Out
- J1-J4 の実装本体（完了前提）。measurement（powered run / floor / verdict / magnitude scorer）。

## Allowed Files
- `src/erre_sandbox/integration/embodied/handoff.py`（Layer2 manifest schema version 定数 additive）
- `tests/test_integration/test_m2_society_selfother.py`（J5 分の guard/golden test 追記）
- `tests/test_integration/test_m2_society_selfother_spend_guard.py`（新規 spend guard、or selfother.py に fold）
- `tests/fixtures/m2_society_selfother_golden/*`（新規 Layer2 golden 4-artifact + expected payload）
- `experiments/20260713-m13-m2-layer2/think_false_parseability_desk_audit.md`（J4 で作成、guard で presence 化可）

## Acceptance Criteria（AC↔test）
- **AC1** Layer2 builder + test の import ∩ {evidence.* / runningness / landscape_divergence 等} = ∅；
  numpy/pandas/scipy/statistics/Counter/groupby/math.log/分布生成 非在；禁止 field 名[magnitude/score/
  confidence/utility/appraisal_state 系]非在（Call/Attribute/field/import root 走査、production executable
  限定、docstring 除外）。
  → `test_self_other_no_measurement_computation`（#12）
- **AC2** 新 LLM call なし → Layer2 on/off で mock adapter call 数が同一（runtime call-count spy、
  exact-oracle fixture）。cap = 既存 per-agent-per-window count。
  → `test_self_other_llm_call_cap`（#13）
- **AC3** Layer2 src（builder + prompt template）に禁止語[mirror neuron/neural mechanism/ミラーニューロン
  実装/神経再現]なし（functional analog のみ、self-scan-aware）。
  → `test_self_other_functional_analog_vocabulary`（#11）
- **AC4** handoff: legacy path byte unchanged + Layer2 N=1 canonical-equivalent + Layer2 golden が
  committed と byte 一致（Windows bake）+ WSL byte 一致（別途実測、cross-platform）。
  → committed golden match test（selfother golden、既存 handoff legacy 回帰は test_m2_society.py が担保）

## Test Plan
- `test_self_other_no_measurement_computation`: Layer2 builder src + selfother test を AST 走査、
  allowlist + aggregation ban + 禁止 field 名 ban。negative fixture（禁止 import/field を trip）。
- `test_self_other_llm_call_cap`: Layer2 on/off で mock adapter call 数を count、同一を assert +
  cap helper 境界。
- `test_self_other_functional_analog_vocabulary`: Layer2 src の identifier tokens + prompt const を
  走査、禁止語なし（self-scan-aware、AC-mandated identifier 例外注記）。
- committed golden: Layer2 on N≥2 golden を fresh bake と byte 一致（+ bake 決定性 2 run）。
- WSL byte 一致: `bash` 経由で WSL 側 pytest 実走 → golden byte match（実測）。

## Stop Conditions
- Layer2 payload に float が残り WSL byte drift → 停止（J2/J3 の float-free 契約破壊、要修正）。
- legacy/off handoff byte 変化 → 停止（§L8 R6）。
- spend guard が docstring の "verdict/floor" 文言で self-trip → 停止（executable AST 限定に修正）。

## Dependencies
- J1-J4 完了が前提（guard は最終コード面を走査、golden は full Layer2 run を bake）。

## Status
NOT_STARTED

## Execution Result
(完了時に記入)
