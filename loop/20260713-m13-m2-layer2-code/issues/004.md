# Issue 004 (J4): continuity gate（exact-oracle boolean wiring）+ SimToM template + think=False desk-audit
verify_level: recheck

## Goal
self-other simulation の continuity gate = 「他者観察 → 自己行動が他者観察に依存」の **causal wiring**
（positive/negative control）を exact-oracle boolean で実証。二点 witness（builder purity[J2 済] +
behavior-dependency）。poison fixture で replay ⊥ causal-fixture 分離を反証可能に固定。runtime
write-spy で episodic memory 非書込み（disjointness）を固定。SimToM prompt template 最終文言確定 +
think=False parseability desk-audit（縮退すれば Layer2 honest 見送り = Layer1 が valid milestone）。

## Background
契約 §L4（continuity 原則 + 二点 witness + 凍結 test 名）+ §L5（replay ⊥ causal-fixture、poison
fixture、HIGH-1/HIGH-3）+ §L6（disjointness runtime write-spy、HIGH-4）+ §L4.2 MEDIUM-3
（`depends_on_other_observation` は test 計算値・LLM 非 emit）+ 文献 §9-ii（think=False 低エントロピー
崩落 risk）。house pattern = test_ecl_bank_continuity.py の `_RetrieveSpy` monkeypatch idiom。
**最重要 anti-over-read**: boolean wiring のみ、magnitude/quality/prediction 非読取（covert scorer 禁止）。

## Scope
### In
- SimToM prompt template 最終文言（builder render の framing const、functional-analog 語彙・bounded）。
- think=False parseability desk-audit doc（`experiments/20260713-m13-m2-layer2/think_false_parseability_desk_audit.md`）。
- continuity gate test（positive/negative + poison replay-causal separation + runtime write-spy disjointness）。
- exact-oracle mock LLM（context 有無で resolved destination を route）+ poison fixture。
### Out
- builder 本体（J2 済）、slot 集約（J3 済）、spend guard/vocab guard/golden（J5）。
- magnitude/quality/prediction 読取（over-read 禁止、covert scorer）。

## Allowed Files
- `src/erre_sandbox/integration/embodied/society.py`（SimToM framing template const additive、builder render 文言）
- `tests/test_integration/test_m2_society_selfother.py`（J4 分の continuity test 追記）
- `experiments/20260713-m13-m2-layer2/think_false_parseability_desk_audit.md`（新規 desk-audit doc）

## Acceptance Criteria（AC↔test）
- **AC1** context present（他者 prior 行動あり、mock LLM が context 有→dest A に route）→ observer の
  resolved behavior が context 依存（ablated baseline B と相異）。`depends_on_other_observation == True`
  （test 計算値・boolean のみ・magnitude 非読取）。
  → `test_self_other_wiring_continuity_positive`（#1）
- **AC2** context ablation（Layer2 off/他者不在/window 0）→ resolved behavior == baseline B。
  `depends_on_other_observation == False`。
  → `test_self_other_wiring_continuity_negative`（#2）
- **AC3** poison fixture（記録済 real-LLM outcome を oracle route と意図矛盾）で走らせ、continuity
  assertion が mock route のみ採用して pass、stored resolved behavior を読む variant は fail。
  → `test_self_other_replay_causal_separation`（#5）
- **AC4** self-other context 経路を走らせながら `_write_observations` / `MemoryStore` 書込みを
  monkeypatch spy で監視、self-other content が episodic memory に一切書込まれないことを runtime assert。
  → `test_self_other_disjointness`（#7）
- **AC5** think=False parseability desk-audit doc が存在し、縮退 risk（文献 §9-ii）+ 縮退時 honest
  見送り path を記録。
  → `test_self_other_wiring_continuity_positive` の SimToM segment parseability 前提 + desk-audit 存在（J5 guard で presence 化 or J4 内 test）

## Test Plan
- positive/negative: exact-oracle mock LLM（context 有→dest A / 無→dest B）で observer の resolved
  destination が context 依存かを boolean 判定。`depends_on_other_observation` は fixture 制御の test 計算値。
- poison: 記録済 outcome を oracle route と矛盾させ、正 variant(mock route のみ)=pass /
  stored-outcome-reading variant=fail を実証。
- disjointness: `_RetrieveSpy` idiom を self-other 向けに拡張（_write_observations/MemoryStore.add 書込み
  監視）、self-other content が memory sink に渡らないことを runtime assert。

## Stop Conditions
- continuity が magnitude/quality を読み始める（covert scorer 化）→ 停止（§L4.1/R2）。
- think=False で SimToM prompt が縮退（parseability 崩壊）→ Layer2 honest 見送り（Layer1 が valid、bounded）。
- disjointness spy が memory 書込みを検出（循環リスク、ES-4 self-anchor collapse 同型）→ 停止。

## Dependencies
- J1（prompt seam）+ J2（builder）+ J3（slot 集約）完了が前提。

## Status
NOT_STARTED

## Execution Result
(完了時に記入)
