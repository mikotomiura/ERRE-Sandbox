# Issue 003: Stage 0 — Δ = T − S の分解とラベル層別 (適用範囲の pin つき)

verify_level: recheck

## Goal

`design-final.md` §1 の `T = 1 − rarity_full` / `S = 1 − rarity_LAO` / `Δ = T − S` を実装し、
**適用範囲が純 max-cos 候補に限られる**ことを test で pin する。
ラベル (good/common) 層別と `S_common − S_good` も出す。

## Background

**Codex HIGH-2**: `Δ = T − S` は `X1` (`agg="mean"`) には成り立たない。
**加えて自前確認 (DA-SC-9)**: `X2` は `_length_controlled` で
`rarity = base − (a + b·len(text))`。length 項は full/LAO で**相殺する**ので
**`Δ(X2) ≡ Δ(X0)`** だが **T/S 個別は復元できない**。
→ 対象は **`X0, X3a, X3b, X3c, C0, C4` の純 max-cos 候補のみ**。

**Codex MEDIUM-2**: LAO 後に survivor が空だと `embed_rarity` は `0.0` を返す。
したがって **`S = 1.0`** となる。この意味論を**凍結して docstring に書く**。

**Codex MEDIUM-1 / MEDIUM-5**: ラベル非依存の平均だけでは AUC collapse の機序を取り逃がす。
**common / good 別に出し、`S_common − S_good` も報告する**。

## Scope

### In
- `split_delta(scores_full, scores_lao, labels, max_sim_full, *, source, candidate, rho) -> DeltaDecomposition`
  - **Codex TASK-PRE HIGH-2 反映**: `DeltaDecomposition` は `source` / `candidate` / `rho` を
    **必須フィールド**として持つが、当初 signature (`scores_full, scores_lao, labels` のみ) では
    **埋められなかった**。3 つを**明示引数**で受け取る。`rho` は I-002 の
    `reference_redundancy` が出した値を**呼び出し側 (I-006) が渡す**。ここで ρ を計算しない
  - `mean_T` / `mean_S` / `mean_delta` を **engaged 項目上**で、**全体 / common / good** の 3 系列
  - `s_common_minus_s_good`
  - **engaged 述語の所有者は `_g1.near_dup_flags_from_similarity()` (Codex TASK-PRE HIGH-1)**。
    `potency_and_near_dup()` は**集約率しか返さず item mask を返さない** (当該 docstring が明記)
    ので、そちらを使うと**再実装せざるを得なくなる**。
    `near_dup_flags_from_similarity(max_sim_full, ref_dedup=_c.REF_DEDUP)` の
    **item-level flag をそのまま使う**。**自前で `>=` 比較を書かない**
- `delta_is_material(delta_int, delta_ext) -> bool` (F4 判定、`DELTA_MATERIAL_EPS`)
- `delta_attribution(T_int, S_int, T_ext, S_ext) -> Literal["T","S","both"]`
  (`|ΔT| > |ΔS|` → T / 逆 → S / 比が `1.0 ± 0.1` → both)

### Out
- τ\* 較正 / セル配線 (I-006) / Stage 1 (I-004) / 判定 (I-005)
- X1 用の別分解式 (**却下済み**、DA-SC-9)

## Allowed Files
- `scripts/paper01_scope_condition.py` (**Δ 分解の節のみ**)
- `tests/test_paper01_scope/test_delta.py` (新規)
- `experiments/20260908-paper01-scope-condition/results/mutation-log-I003.md` (新規、MEDIUM-5)

## Acceptance Criteria

| AC | 条件 | test |
|---|---|---|
| 003-1 | max-agg で `Δ = T − S` が **`embed_rarity` の実測と厳密一致** (stub vmap で) | `test_delta_matches_embed_rarity_max_agg` |
| 003-2 | **survivor 空**のとき `S == 1.0` (rarity 0.0 の意味論) | `test_empty_survivor_gives_full_coverage` |
| 003-3 | **`Δ(X2) ≡ Δ(X0)`** — length 項が full/LAO で相殺する | `test_length_control_cancels_in_delta` |
| 003-4 | **X1 (mean-agg) は対象外** — `DELTA_TS_CANDIDATES` に含まれず、渡すと明示的に拒否される | `test_mean_agg_candidate_is_rejected` |
| 003-5 | engaged 述語が `potency_and_near_dup` と**同じ項目集合**を選ぶ | `test_engaged_predicate_matches_potency` |
| 003-6 | ラベル層別 — common / good の平均が全体平均と整合し、片方だけの入力でも壊れない | `test_label_stratified_means` |
| 003-7 | `S_common − S_good` が手計算値と一致 | `test_s_common_minus_s_good` |
| 003-8 | `delta_attribution` が T / S / both の**3 出力すべてに到達可能** (非恒真) | `test_delta_attribution_all_branches_reachable` |
| 003-9 | `delta_is_material` の境界 — `eps` ちょうどで**どちらに倒れるか**が定義どおり | `test_delta_material_boundary` |
| **003-MUT** | 境界述語 (`>` vs `>=`、`1.0 ± 0.1` の両端、`T`/`S` の引き算の向き、engaged 述語の閾値、平均の分母) を**機械的に総当たり**して全 kill | `results/mutation-log.md` |

## Test Plan
- **encoder 非依存** — stub vmap (手組みの単位ベクトル辞書) で
  `make_embed_candidate` / `embed_rarity` を実際に通す。
- 003-1 / 003-3 は**実際に凍結関数 `_diag.embed_rarity` を呼んで**突き合わせる
  (期待値のハードコードだけにしない)。
- 003-8 は 3 出力それぞれに**到達する fixture** を置く。
- **配線の検査に AUC を使わない**。

## Stop Conditions
- `Δ = T − S` が max-agg でも実測と合わない → **停止して ADR へ差し戻し** (分解が誤り)
- 凍結 `embed_rarity` の改変が要る → **S3**

## Dependencies
- I-001
- **I-001b (共有契約の穴埋め + 節 anchor)** — Codex TASK-PRE HIGH-3/4/5 + MEDIUM-1 反映。
  **これの完了前に着手しない** (契約が変わるため)

## TASK-PRE 反映 (Codex `_codex-preplan.md`、Verdict Revise)

### 追記位置 (MEDIUM-1、**衝突回避のため binding**)

`scripts/paper01_scope_condition.py` には I-001b が節 anchor を置いてある。
本 issue のコードは **自分の anchor 直下にのみ**追記する:

```python
# ---------------------------------------------------------------------------
# I-003: Stage 0 -- Delta = T - S decomposition
# ---------------------------------------------------------------------------
```

**ファイル末尾 (EOF) に追記しない。** 4 branch が同じ EOF を触ると merge conflict になる。

### mutation log の分離 (MEDIUM-5)

本 issue の mutation log は **`experiments/20260908-paper01-scope-condition/results/mutation-log-I003.md`**
に書く (共有の `mutation-log.md` に直接書かない)。統合は I-007 が行う。


## Status
done

## Execution Result

### base 検証

worktree HEAD が `3ab8549` (Merge I-005) と一致していなかった (working tree
はクリーンだったが履歴に `3ab8549` を含んでいなかった) ため、着手前に
`git reset --hard 3ab8549` を実行してから開始した (この worktree にはまだ
作業が無かったため破棄して問題なし)。実行後: anchor 5 個 (`I-002`〜`I-006`)
存在・`3ab8549` が履歴に含まれることを確認済み。

### 実装内容

`scripts/paper01_scope_condition.py` の `# I-003: Stage 0 -- Delta = T - S
decomposition` 節 (自分の anchor 直下のみ、EOF 追記なし) に実装:

- `split_delta(scores_full, scores_lao, labels, max_sim_full, *, source,
  candidate, rho) -> DeltaDecomposition` — issue 本文の signature どおり
  `source`/`candidate`/`rho` を明示引数で受け取る (ここで ρ は計算しない)。
  `candidate` が `DELTA_TS_CANDIDATES` (`X0, X3a, X3b, X3c, C0, C4`) の
  メンバでなければ `ValueError` (X1/X2 を明示的に拒否、DA-SC-9)。
  **engaged 述語は `_g1.near_dup_flags_from_similarity(max_sim_full,
  ref_dedup=_c.REF_DEDUP)` の item-level flag をそのまま使用**
  (`potency_and_near_dup` は集約率しか返さないため使わない、
  Codex TASK-PRE HIGH-1 の指示どおり)。engaged 項目のみを good/common に
  層別し、`T = 1 - scores_full` / `S = 1 - scores_lao` /
  `Δ = T - S` を計算、各クラスの平均 (`mean_t_good` 等 6 種) と
  `s_common_minus_s_good` を返す。空クラスは `0.0` にフォールバック
  (`potency_and_near_dup` の `if good_mask.any() else 0.0` と同じ規約)。
- `delta_is_material(delta_int, delta_ext) -> bool` — F4 判定の陽性形
  (`abs(delta_int - delta_ext) >= DELTA_MATERIAL_EPS`)。境界値ちょうどで
  material 側に倒す (design-final.md §3 の "無い" 判定が厳密 `<` なので、
  その否定は `>=`)。
- `delta_attribution(t_int, s_int, t_ext, s_ext) -> str` — `"T"`/`"S"`/
  `"both"` の 3 分岐、非恒真。both 判定 (`1.0 ± 0.1`、両端含む) を先に
  チェックしてから T/S の強弱比較へフォールスルーする構造。

新規モジュール定数 `_T_S_ATTRIBUTION_TOLERANCE`
(= 0.1、design-final.md §3 "1.0 ± 0.1" の直接表現) を同節内に追加。
`DELTA_TS_CANDIDATES` / `DELTA_MATERIAL_EPS` / `DeltaDecomposition` は
I-001/I-001b が既に凍結済みのものを import して使うのみで、再定義していない。

**型注釈は `tuple[float, ...]` / `tuple[int, ...]` / `str` のみを使用**し、
`Sequence`/`Literal` 等の新規 import を一切追加していない
(モジュール先頭の import 節・`from typing import ...` 行は無改変)。

### verify (4 段、pin 済みコマンドをそのまま採用)

1. `pytest -q tests/test_paper01_scope` → **exit 0**
   (**49 passed, 0.21s** — 既存 39 test + 本 issue の新規 10 test)
2. `mypy src` → **exit 0** (247 files, Success)
3. `ruff check src tests scripts/paper01_scope_condition.py` → **exit 0**
   (All checks passed)
4. `ruff format --check src tests scripts/paper01_scope_condition.py` →
   **exit 0** (598 files already formatted)

(DA-SC-20 により per-issue はフルスイートを回さない。統合時にオーケストレータが
1 回だけ回す。)

### test (`tests/test_paper01_scope/test_delta.py`, 新規 10 test)

AC 003-1〜003-9 に 1:1 対応 (003-1 は本体 test に加えて T-S 方向専用の
`test_delta_is_t_minus_s_not_s_minus_t` を追加、計 10 本):

- `test_delta_matches_embed_rarity_max_agg` (003-1) — 凍結
  `_diag.embed_rarity` を実際に呼んで得た T/S/Δ を独立に手計算し、
  `split_delta` の出力と突き合わせ (期待値をハードコードしない)
- `test_delta_is_t_minus_s_not_s_minus_t` — Δ の符号方向の単独 pin
- `test_empty_survivor_gives_full_coverage` (003-2) — 単一 anchor の
  完全一致アイテムで LAO survivor 空を作り、`embed_rarity` の実測 `0.0`
  が `S = 1.0` になることを確認
- `test_length_control_cancels_in_delta` (003-3) — `_diag._length_controlled`
  で実際に X2 candidate を構築し、Δ(X2) と Δ(X0) が一致 (T/S 個別は不一致)
  することを確認。X2 を `split_delta` に渡すと拒否されることも同テストで確認
- `test_mean_agg_candidate_is_rejected` (003-4) — X1/X2 双方の拒否
- `test_engaged_predicate_matches_potency` (003-5) — engaged 選択集合が
  `_g1.potency_and_near_dup` の内部 near_dup マスクと同じ項目集合を選ぶこと
  を、その集約率経由でクロスチェック (非恒真: 一部アイテムが実際に除外される
  fixture)
- `test_label_stratified_means` (003-6) — good のみ engaged の一方的入力で
  壊れないこと、混合入力で加重平均が全体平均と整合すること
- `test_s_common_minus_s_good` (003-7) — 手計算値との一致
- `test_delta_attribution_all_branches_reachable` (003-8) — T/S/both
  の 3 分岐すべてに到達 (両端境界値 0.9/1.1 とその外側近傍も明示 pin)
- `test_delta_material_boundary` (003-9) — eps ちょうど/直下/直上の境界

### mutation testing (`results/mutation-log-I003.md`)

15 mutant を機械的に列挙 (境界述語 `>`/`>=`、`1.0±0.1` 両端、T/S の引き算の
向き、engaged 述語の閾値 (flag 側・REF_DEDUP 側の両方)、平均の分母、ラベル
層別の割当、適用範囲ゲート、`s_common_minus_s_good` の向き、連言の
`and`/`or`)。各 mutant は実際にファイルへ適用 → pytest 実行 → 復元 →
SHA-256 で復元一致を確認、を自動化したハーネスで実施。

**結果: 14/14 (required-kill set) KILLED、生存 0**。加えて 1 件
(M4: `delta_attribution` 最終行の `>` vs `>=`) は **数学的に到達不能な
同値 mutant** であることを確認した上で診断用に実行し、期待どおり
SURVIVED (生存) することを確認・ログに理由付きで明記 (`delta_t == delta_s`
(非ゼロ) は常に ratio=1.0 となり、既に凍結済みの `1.0±0.1` both 判定が
先に "both" を返すため、この行の `>`/`>=` 差異を区別できる入力が
実装上存在しない)。

**mutation ハーネス自体のバグを実際に検出・修正した実例**: M8 (engaged
述語の `REF_DEDUP` 閾値) の初期実装は `old`/`new` 文字列がファイル内に
2 箇所 (docstring とコード本体) 存在することに気づかず、
`str.replace(..., 1)` が docstring の方だけを書き換えて SURVIVED
(偽陰性) を出した。ハーネスの `expected` vs 実測 verdict の突き合わせで
`MISMATCH` として検出し、コード本体を一意に指す文字列に修正して
再実行、KILLED を確認した。`feedback_witness_needs_mutation_testing`
が要求する「実際に当てて確かめる」の実例。

**別途、手動デバッグ中に mutation を復元し損ねる事故が 1 回発生**
(バックアップファイルが `||` の短絡評価で作られていなかった) が、
`grep` で即座に検出し `Edit` で正確に復元、4 段 verify 全緑 +
`git diff --stat` で意図した挿入のみであることを確認した。詳細は
mutation log 本文に honest に記録済み。

### Allowed Files 逸脱

なし。`git status --porcelain` で確認した変更ファイルは Allowed Files
3 件とちょうど一致:
- `scripts/paper01_scope_condition.py` (Δ 分解の節のみ)
- `tests/test_paper01_scope/test_delta.py` (新規)
- `experiments/20260908-paper01-scope-condition/results/mutation-log-I003.md` (新規)

`design-final.md` は無改変。凍結済み dataclass (`DeltaDecomposition` 含む)
は再定義せず import のみ。`import numpy as np` はモジュール先頭の既存を
再利用 (再追加していない)。X1 用の別分解式は作っていない (却下済み、DA-SC-9)。

### 残課題

- τ\* 較正・セル配線 (I-006)、Stage 1 (I-004)、判定 (I-005 は完了済み) は
  本 issue の Scope Out どおり対象外。
- `split_delta` の呼び出し側 (I-006/I-007) がどの型 (`list`/`tuple`) で
  `scores_full`/`scores_lao`/`labels`/`max_sim_full` を渡すかは未確定。
  本関数は `tuple[float, ...]`/`tuple[int, ...]` で型付けしている
  (`DrawItems` の既存フィールドと同じ規約)。呼び出し側が `list` を渡す
  設計になる場合は呼び出し側で `tuple(...)` 変換が必要になる可能性がある
  (mypy は `scripts/` を対象にしないため実害は無いが、統合時に確認推奨)。
