# Issue 007: 実走一式 + `run.sh` (1 コマンド再現) + `notes.md` (honest verdict)

verify_level: recheck

## Goal

`design-final.md` の測定を**実際に走らせ**、`run.sh` で 1 コマンド再現できるようにし、
**得られた verdict をそのまま** `notes.md` に honest に書く。**Done = D1-D6** (`requirement.md`)。

## Background

**Done は「条件 C が支持された」ことではない** (DA-SC-4)。
**「事前登録した測定が完走し、凍結済み判定関数が全域で verdict を出した」**ことである。
**結果が期待と逆でも `design-final.md` を開けない** (S1/S2)。

G1 の反省を binding で持ち込む:
- **CI を回している最中にリポジトリのファイルを触らない** (G1 の最終 CI は mutation と
  並走して信用できず回し直した)
- **長時間実走の完了判定は PID 生存 + log 末尾 completion marker + `nvidia-smi` idle**。
  tqdm 最新行を使わない (`feedback_log_tail_completion_marker`)
- **mutation の後に「出力 JSON を読んで数字の意味を突き合わせる」工程を明示的に置く**
  (G1 では全 test 緑・全 mutant kill の状態で 2 件の意味欠落が残り、
  出力 JSON の数字を読んで初めて見つかった)

## Scope

### In
- `experiments/20260908-paper01-scope-condition/run.sh`
  1. `PYTHONHASHSEED` (= `SEED`) / `HF_HUB_OFFLINE=1` / `PYTHONUTF8=1` を export
  2. `--fidelity` (**fidelity pin (b)+(c)**、不一致で exit != 0)
  3. `--scope` → `results/scope.json`
  4. `--scope` を scratch へ 2 回目 → **SHA-256 一致ゲート** (不一致で exit 1)
  5. `metrics.json` (読み取り専用抽出。統計を再計算しない)
  6. completion marker を最終行に出す
- `experiments/20260908-paper01-scope-condition/{run_gate.py,build_metrics.py}`
- `experiments/20260908-paper01-scope-condition/notes.md`
  — 仮説 / 手順 / **結果 (honest)** / **Limitations (design-final §8 の 8 項目必須)** / 逸脱・気づき
- `results/mutation-log.md` の統合 (**全 issue 分**)
- `.steering/.../blockers.md` と `tasklist.md` の最終化

### Out
- 論文本文・タイトル・repo 名の変更 (**B-G1-5 は「書く」枝に入った場合のみ**、別タスク)
- `design-final.md` の変更 (**結果を見た後の変更は S1/S2**)

## Allowed Files
- `experiments/20260908-paper01-scope-condition/**` (`results/` 含む)
- `.steering/20260908-paper01-scope-condition/{blockers.md,tasklist.md}`
- `scripts/paper01_scope_condition.py` (**CLI の `--fidelity` 分岐のみ**、必要なら)

## Acceptance Criteria

| AC | 条件 | test / 検証 |
|---|---|---|
| 007-1 | `bash run.sh` が **exit 0** で完走する | run.log 末尾の completion marker + exit code |
| 007-2 | **2 回実行で `scope.json` が SHA-256 一致** | `run_gate.py check-hashes` の exit code |
| 007-3 | **fidelity pin (b)+(c) が緑** — G1 ARM-A と内部 C0/C4 が再現 | `--fidelity` の exit code |
| 007-4 | **Ocsai 到達不能なら verdict を出さず exit != 0** (DA-SC-5) | `test_ocsai_unavailable_exits_nonzero` |
| 007-5 | `scope.json` に **Stage 0 / Stage 1 / 全セル / `ScopeVerdict` / セル別 `object_weights`** が載る | `test_scope_json_shape` (I-006) + 実物の目視突き合わせ |
| 007-6 | `notes.md` に **Limitations 8 項目**が漏れなく載る | `test_notes_limitations_complete` (marker grep) |
| 007-7 | **CI parity 4 段 pass** (`ruff format --check` / `ruff check` / `mypy src` / `pytest -q`) | `pwsh scripts/dev/pre-push-check.ps1` の exit code |
| 007-8 | **mutation 後の意味突き合わせ工程**を実施し、`notes.md` に「出力 JSON のどの数字を何と突き合わせたか」を書く | `notes.md` の該当節 |
| 007-9 | `notes.md` の結果が **`scope.json` の実値と一致** (盛らない・丸めない) | 目視突き合わせ + `build_metrics.py` の抽出値 |

## Test Plan
- **実走中はリポジトリのファイルを触らない。** mutation testing と CI を**並走させない**。
- 完了判定は **PID 生存 + `run.log` 末尾の completion marker**。tqdm の最新行を使わない。
- 007-9 は **`scope.json` を実際に読んで** `notes.md` の各数字を 1 つずつ突き合わせる。
  **これが「全 test 緑・全 mutant kill でも意味が壊れている」型の欠陥を捕まえる最後の関門。**

## Stop Conditions
- 結果を見てから `design-final.md` / 判定規則 / 凍結値を変えたくなった → **S1 / S2 で即停止**
- `cell(τ=0.90,k=30)` が G1 と一致しない → **停止** (I-006 の Stop と同じ)
- 凍結 apparatus / `paper01_external_audit.py` の改変が要る → **S3**
- Ocsai が長時間到達不能 → 停止して user 裁定 (verdict は出さない)

## Dependencies
- **I-006** (全部の合流点)
- **I-001b (共有契約の穴埋め + 節 anchor)** — Codex TASK-PRE HIGH-3/4/5 + MEDIUM-1 反映。
  **これの完了前に着手しない** (契約が変わるため)

## TASK-PRE 反映 (Codex `_codex-preplan.md`、Verdict Revise)

### 追記位置 (MEDIUM-1、**衝突回避のため binding**)

`scripts/paper01_scope_condition.py` には I-001b が節 anchor を置いてある。
本 issue のコードは **自分の anchor 直下にのみ**追記する:

```python
# ---------------------------------------------------------------------------
# I-007 はこのファイルの CLI 分岐のみ
# ---------------------------------------------------------------------------
```

**ファイル末尾 (EOF) に追記しない。** 4 branch が同じ EOF を触ると merge conflict になる。

### mutation log の分離 (MEDIUM-5)

本 issue の mutation log は **`experiments/20260908-paper01-scope-condition/results/mutation-log-I007.md`**
に書く (共有の `mutation-log.md` に直接書かない)。統合は I-007 が行う。

### CLI 境界と mutation log の統合 (Codex TASK-PRE MEDIUM-4 / MEDIUM-5)

- `--fidelity` の **parser 自体は I-006 が予約済**。本 issue は**分岐の中身のみ**を実装する。
- `results/mutation-log-I00X.md` が issue ごとに分かれている。本 issue が
  **`results/mutation-log.md` に統合**する (各 issue の節を順に束ねる。**要約しない**)。


## Status
in-progress (007a done / 007b = 実走 pending)

## Execution Result

**本節は 007a (実走の前段) の分のみ。007b (実走、別セッション、user 裁定を
経て実施) の欄は空けておく。**

### base 検証
worktree は main (`9a13b6c`、scope-condition 作業なし) から始まっていたため
`git reset --hard cf501ff` (Merge I-006) を実施。anchor 5 個 / `cf501ff`
履歴内に存在 / `85 passed, 1 skipped` を確認 (指示どおり)。

### 実装内容 (Scope In のうち 007a が担う部分)
- **`--fidelity` の中身** (`cli_fidelity()`): pin (c) (内部 C0/C4) は凍結済み
  `_g1.run_fidelity()` にそのまま委譲、pin (b) (外部 ARM-A 再現) は
  `measure_g1_arm_a_fidelity()` が `build_cell`/`score_cell` を通して
  `test_dense_cell_reproduces_g1_arm_a` と同じ配線を実行し
  `g1_arm_a_fidelity_matches()` で凍結値と比較。**「データが無い」
  (`FIDELITY_EXIT_SOURCE_UNAVAILABLE`=2) と「不一致」
  (`FIDELITY_EXIT_MISMATCH`=1) を別 exit code で区別**し、両方発生した
  場合は不一致を優先して報告する。
- **`--scope` の Cambridge セル配線**: `build_split_cells()` が Ocsai 各
  split の Stage 2 2x2 グリッド (DA-SC-3 の 4 セル共通 active 交差 +
  MEDIUM-3 の τ* 隣接段チェック) を配線し、`_gather_cambridge()` /
  `_score_cambridge_arm_s()` が Cambridge の単一 ARM-S セルを
  (SPLIT-BLOCK の τ*/k* で、本 issue 独自の設計選択として) スコアし、
  `cli_scope()` がこれらと Stage 0/Stage 1 を組み立てて凍結済み
  `run_scope()` を呼ぶ。実データ欠如時は全経路で `source_unavailable` +
  `exit 1` (verdict を出さない、DA-SC-5)。
- **`experiments/20260908-paper01-scope-condition/run.sh`**:
  ①環境変数 export → ② `--fidelity` → ③ `--scope` (1回目) →
  ④ `--scope` (2回目・scratch) + `run_gate.py check-hashes` →
  ⑤ `build_metrics.py` (読み取り専用抽出) → ⑥ completion marker、の順。
  **書いただけで実行していない**。
- **`run_gate.py` / `build_metrics.py`**: `experiments/20260907-paper01-g1/`
  の同名スクリプト (issue 008 の成果物) をほぼそのまま踏襲。8 項目
  Limitations 完全性チェック + DA-SC-19 事前登録修正マーカーチェックを
  独自に追加。
- **`notes.md`**: 仮説 / 借用 apparatus / 手順 / I-007a の境界 / データ欠如
  と不一致の区別 / unit test での完走証明 / Limitations 8 項目 (design-final
  §8) + DA-SC-19 事前登録修正の明記 / 逸脱・気づき、を記載。
  **「結果」節は `(未実走 — I-007b で埋める)` のプレースホルダのみ**。

### `--scope` と `--fidelity` の exit code (実データ無しでの実測確認)
本サンドボックス環境には Ocsai が無く、ネットワークアクセスは禁止のため、
`--scope` / `--fidelity` の pin (b) は実際に走らせれば必ず
source-unavailable 経路を通る。**「データが無い」と「不一致」はコード上
別々の bool 変数 (`mismatch` / `unavailable`) で管理され、混同されない**
ことを、単体テスト (`test_cli_fidelity_pin_c_source_unavailable_is_not_a_mismatch`
等) で確認済み。ただし本セッションでは実際に CLI を実行してこれを観測
してはいない (`--fidelity`/`--scope` を実プロセスとして起動していない) ---
コード上の分岐・単体テストでの確認に留まる。

### 4 つの verify の exit code
- `pytest -q tests/test_paper01_scope`: exit 0 (129 passed, 1 skipped)
- `mypy src`: exit 0 (247 files)
- `ruff check src tests scripts/paper01_scope_condition.py`: exit 0
- `ruff format --check src tests scripts/paper01_scope_condition.py`: exit 0
  (実行前に `ruff format` で本セッションの新規/変更ファイルのみ整形済み)

### mutation testing
7 mutant (issue 必須の 3 種 M1-M3 + 追加 4 種 M4-M7)、**7/7 KILLED、生存 0**。
3 ファイル (`scripts/paper01_scope_condition.py` /
`experiments/20260908-paper01-scope-condition/{run_gate.py,build_metrics.py}`)
とも mutant 適用後に SHA-256 で pristine 復元を確認済み。詳細は
`experiments/20260908-paper01-scope-condition/results/mutation-log-I007a.md`。

### 実走・ネットワーク取得の確認
**本セッションでは Ocsai の取得・ネットワークアクセスを一切行っていない。
`run.sh` を実行していない。`results/scope.json` を実データで生成していない。
verdict を一切出していない。**

### commit
worktree のブランチに 1 commit (SHA は最終報告で提示)。push / PR 作成なし。

### 残課題 (007b へ引き継ぎ)
- Ocsai の再取得 (ネットワーク、user 明示認可が必要) と実走
- `notes.md` の「結果」節を実測値で埋める (honest、盛らない・丸めない)
- `results/mutation-log.md` への全 issue 分の統合
- `.steering/20260908-paper01-scope-condition/{blockers.md,tasklist.md}`
  の最終化
- 007-1/007-2/007-3/007-4/007-5/007-8/007-9 (実走を要する AC) の充足確認
  (007-6/007-7 相当は 007a で先行して緑を確認済み)
