# Issue 002: 疎化層 — leader clustering と冗長性 ρ (fidelity pin (a) つき)

verify_level: recheck

## Goal

`design-final.md` §2/§3 の **leader clustering** と **冗長性 ρ** を実装し、
**`leader_cluster_indices(radius=REF_DEDUP, cap=n)` が既存
`greedy_dedupe_indices(cap=n)` と返り index 列まで完全一致する**ことを pin する
(**fidelity pin (a)**)。

## Background

条件 C は「参照集合が**クラスタ代表 1 件ずつで非冗長に覆う canonical なリスト**であるとき」
という**構成**の命題であり、それを外部プール上で作る唯一の装置がこの層である。

**Codex HIGH-4**: 「`leader_cluster ≡ greedy_dedupe` の一致だけでは ARM-A 再現にならない」
— それは正しいが、**この issue が担うのはあくまで (a) の一致まで**である。
セル全体の順序 (`dedupe → cap → draw`) と ARM-A 再現 (fidelity pin (b)) は **I-006** の責務。

**Codex HIGH-1 / MEDIUM-1 (反映済)**: ρ は **S の proxy であって代数的対応物ではない**。
docstring に必ずそう書く。「代数的に対応」と書かない。

## Scope

### In
- `leader_cluster_indices(embeddings, *, cap, radius) -> list[int]`
  - 凍結走査順・単位正規化済み入力前提・`cos.max() < radius` のとき keep・`cap` で打ち切り
  - **既存 `greedy_dedupe_indices` と同一アルゴリズム**の閾値可変版
- `reference_redundancy(embeddings) -> float` = `mean_r max_{r'≠r} cos(r, r')`
  - **`|R_o| < 2` のときは `0.0` を返す** (Codex TASK-PRE LOW-1 反映で値を確定)。
    I-006 の τ\* 較正入力になるため、**issue 側で値を固定**し docstring にも書く
- `corpus_redundancy(by_object) -> float` = **物体横断の単純平均** (物体重みで加重しない)

### Out
- τ\* の較正 (I-006) / セル配線 (I-006) / Δ 分解 (I-003)
- **`InternalReference` を埋めること (I-006 の責務、Codex TASK-PRE HIGH-5)** —
  I-001 のコード docstring が「I-002 が埋める」と書いていたのは**誤り**で、
  I-001b が I-006 所有に訂正する。**I-002 は低水準の ρ 関数だけを持つ**
- `paper01_external_audit.py` の編集

## Allowed Files
- `scripts/paper01_scope_condition.py` (**疎化層の節のみ**)
- `tests/test_paper01_scope/test_sparsify.py` (新規)
- `experiments/20260908-paper01-scope-condition/results/mutation-log-I002.md` (新規、MEDIUM-5)

## Acceptance Criteria

| AC | 条件 | test |
|---|---|---|
| 002-1 | **fidelity pin (a)** — 無作為な単位ベクトル群 100 ケースで `leader_cluster_indices(radius=REF_DEDUP, cap=n)` == `greedy_dedupe_indices(cap=n)` (**index 列が完全一致**) | `test_leader_cluster_matches_greedy_dedupe_at_ref_dedup` |
| 002-2 | **恒真でない** — `radius` を `REF_DEDUP` から動かすと 002-1 が**落ちるケースが存在する** | `test_leader_cluster_differs_at_other_radius` |
| 002-3 | `radius` 単調性 — `radius` を下げると keep 数は**増えない** | `test_leader_count_is_monotone_in_radius` |
| 002-4 | `cap` が効く — `cap=k` なら返り値長 ≤ k、かつ先頭 k 件は `cap=∞` の先頭 k 件と一致 | `test_cap_truncates_prefix` |
| 002-5 | 走査順依存 — 入力順を入れ替えると別の代表集合になりうる (凍結走査順が load-bearing) | `test_leader_cluster_depends_on_scan_order` |
| 002-6 | ρ が既知配置で手計算値と一致 (直交集合 → 0 付近 / 全同一 → 1) | `test_reference_redundancy_known_configurations` |
| 002-7 | ρ の縮退 — `|R_o| < 2` で凍結値を返す | `test_reference_redundancy_degenerate` |
| 002-8 | ρ は**最大ペア間 cos ではない** — 1 ペアだけ密で他が疎な集合で両者が乖離する | `test_rho_is_not_max_pairwise` |
| 002-9 | `corpus_redundancy` は**単純平均**であり物体重みで加重しない | `test_corpus_redundancy_is_unweighted` |
| **002-MUT** | 境界述語 (`<` vs `<=`、`max` vs `min`、`cap` 比較、`r' ≠ r` の除外、平均の分母) を**機械的に総当たり**して全 kill | `results/mutation-log.md` |

## Test Plan
- **encoder 完全非依存** — `np.random.default_rng(SEED)` の単位ベクトルと手組み配置のみ。
- 002-1 は **`greedy_dedupe_indices` を実際に呼んで比較**する
  (期待値をハードコードしない = 恒真回避)。
- 002-8 は **`rho != max_pairwise` を実際に assert** する
  (design-final §2.2 の主張がコードで成立していることの witness)。
- **配線の検査に集約統計を使わない** — `radius` がどこに渡ったかは
  **spy で生の呼び出し引数を見る**。

## Stop Conditions
- `greedy_dedupe_indices` と一致させるために**既存関数の編集**が要る → **S3**
- 正規化前提が外部プールで成り立たないと判明 → 停止して ADR へ差し戻し

## Dependencies
- I-001 (定数が凍結されていること)
- **I-001b (共有契約の穴埋め + 節 anchor)** — Codex TASK-PRE HIGH-3/4/5 + MEDIUM-1 反映。
  **これの完了前に着手しない** (契約が変わるため)

## TASK-PRE 反映 (Codex `_codex-preplan.md`、Verdict Revise)

### 追記位置 (MEDIUM-1、**衝突回避のため binding**)

`scripts/paper01_scope_condition.py` には I-001b が節 anchor を置いてある。
本 issue のコードは **自分の anchor 直下にのみ**追記する:

```python
# ---------------------------------------------------------------------------
# I-002: sparsification layer (leader clustering / rho)
# ---------------------------------------------------------------------------
```

**ファイル末尾 (EOF) に追記しない。** 4 branch が同じ EOF を触ると merge conflict になる。

### mutation log の分離 (MEDIUM-5)

本 issue の mutation log は **`experiments/20260908-paper01-scope-condition/results/mutation-log-I002.md`**
に書く (共有の `mutation-log.md` に直接書かない)。統合は I-007 が行う。


## Status
done

## Execution Result

**実装**: `scripts/paper01_scope_condition.py`「I-002: sparsification layer」節
(anchor 直下、自分の anchor 直下のみに追記、EOF 追記なし) に 3 関数を実装した。

- `leader_cluster_indices(embeddings, *, cap, radius)` -- `greedy_dedupe_indices`
  と同一の凍結走査順 greedy 判定 (`cos.max() < radius` で keep) を、閾値を
  `radius` 引数として可変にした twin。**fidelity pin (a)**: `radius=REF_DEDUP`
  で `greedy_dedupe_indices(cap=n)` と index 列が完全一致することを
  `test_leader_cluster_matches_greedy_dedupe_at_ref_dedup` (100 ランダム
  単位ベクトル battery, 期待値は実際に `greedy_dedupe_indices` を呼んだ結果)
  で pin した。docstring に「これは pin (a) のみであり ARM-A のセル全体
  (dedupe→cap→draw, fidelity pin (b)) の再現ではない。所有者は I-006」と
  明記 (Codex HIGH-4 反映、issue Background のとおり)。
- `reference_redundancy(embeddings) -> float` -- §2.1
  `rho(R_o) = mean_r max_{r'≠r} cos(r,r')`。`|R_o| < 2` は凍結値 `0.0`
  (Codex TASK-PRE LOW-1)。docstring に「rho は S の proxy であり代数的
  対応物ではない」を verbatim で明記 (Codex HIGH-1/MEDIUM-1、「代数的に
  対応」という表現はコード中どこにも書いていない)。
- `corpus_redundancy(by_object) -> float` -- 物体横断の**単純平均**
  (物体重みで加重しない)。空 mapping は `0.0`。

**追加した import**: モジュール先頭 (typing import の直下、`paper01_external_audit.py`
と同じ位置・慣習) に `import numpy as np` を 1 行追加。I-002 の関数群が
`np.ndarray` を実際に演算に使うために必須で、E402 (module-level import
not at top of file) を避けるため import ブロックへ置いた。既存の
6 dataclass 契約や I-001/I-001b のロジックには一切触れていない。

**verify (4 コマンド、foreground、exit code)**:
1. `pytest -q tests/test_paper01_scope` -- **exit 0**、27 passed
   (既存 15 + 新規 12: AC 002-1〜002-9 の 9 本 + 境界支援 3 本)
2. `mypy src` -- **exit 0**、247 files (mypy は `src` のみが対象で
   `scripts/` は対象外だが、既存ファイルと同じ完全型注釈スタイルは維持した)
3. `ruff check src tests scripts/paper01_scope_condition.py` -- **exit 0**
   (途中 D205 の docstring 空行違反を 1 件検出→修正して green)
4. `ruff format --check src tests scripts/paper01_scope_condition.py` --
   **exit 0**、596 files already formatted

**AC↔test 対応** (test_sparsify.py、全て encoder 非依存・ハードコード
期待値なし):
- 002-1 `test_leader_cluster_matches_greedy_dedupe_at_ref_dedup`
- 002-2 `test_leader_cluster_differs_at_other_radius`
  (手組み battery: A(0°)/D(60°,cos=0.5)/C(90°) — REF_DEDUP=0.90 では
  `[0,1,2]`、radius=0.30 では `[0,2]` と分岐することを実測)
- 002-3 `test_leader_count_is_monotone_in_radius` -- **重要な訂正**:
  乱数 battery で `radius` 単調性を検証しようとしたところ、**20000 試行の
  探索で ~1% (n=13〜14, dim=6〜7 台) が非単調になる実例を発見した**
  (greedy の kept 集合が radius ごとに変わるため、path-dependence で
  「小さい radius の方が keep 数が多い」逆転が稀に起きる — 数学的な
  全域定理ではない)。よって**手組みの決定論的 fixture** (90° 離れた
  2 クラスタ、片方は全 TAU_GRID で常に併合、もう片方は cos=0.62 ちょうどで
  τ=0.65→0.60 の境目で 1 段だけ併合) に切り替え、`counts ==
  [3,3,3,3,3,3,2,2,2,2,2]` を厳密 pin した。この「乱数では稀に破れる」
  事実は test docstring と本節に honest に残す (over-claim 回避)。
- 002-4 `test_cap_truncates_prefix`
- 002-5 `test_leader_cluster_depends_on_scan_order` (A/B near-dup pair の
  スキャン順を入れ替えると生き残る代表ベクトルの実体が変わることを
  `.tobytes()` 比較で確認 — index 列自体は偶然 `[0,2]` で一致するため
  index 列比較ではなく実ベクトル内容で判定する設計にした)
- 002-6 `test_reference_redundancy_known_configurations` (直交基底→0.0
  厳密一致、全同一→1.0 厳密一致、共に浮動小数の丸め誤差なしで exact)
- 002-7 `test_reference_redundancy_degenerate` (n=0, n=1 → 0.0)
- 002-8 `test_rho_is_not_max_pairwise` (4D: 密ペア cos=0.99 + 直交2点、
  rho=0.495 ≠ max_pairwise=0.99 を実際に assert)
- 002-9 `test_corpus_redundancy_is_unweighted` (2-ref/6-ref オブジェクト、
  単純平均 0.5 ≠ 加重平均 0.25 を実際に assert)
- 補助 (002-MUT 用の境界 witness、AC 番号は持たないが mutation を直接支える):
  `test_leader_cluster_boundary_equality_is_excluded` (`cos==radius` 厳密境界)、
  `test_reference_redundancy_two_refs_not_degenerate` (`n==2` は非縮退)、
  `test_corpus_redundancy_empty_is_zero`

**mutation testing (`results/mutation-log-I002.md`)**: issue指定の4カテゴリ
(`<` vs `<=`、`max` vs `min`、`cap` 比較、`r'≠r` の除外) + 平均の分母 2箇所
を機械的に列挙し、計 **7 mutant を実際に当てて実行し観測**
(`assert old in s` / `assert new != old` / 書込み後 `assert new in on_disk`
の二重確認 + 復元後 SHA-256 byte-identical 確認、スクリプトは
scratchpad の driver で毎回 pristine から差分適用→pytest 実行→復元)。
**7/7 KILLED, 0 survivors**。**過大申告の是正 2 件を実測で確認**:
M1 (`<`→`<=`) は AC 002-1/002-2 のランダム battery では**キルされず**、
専用の厳密等号境界 fixture (`test_leader_cluster_boundary_equality_is_excluded`)
のみがキルすることを確認したうえでログに記載 (前 issue の反省
「witness が自分の効き方を過大申告しないこと」を適用)。M6 (`n<2`→`n<=2`)
も同様に、`n=2` 専用境界テストのみがキルすることを確認。

**Allowed Files 逸脱**: なし。触れたのは
`scripts/paper01_scope_condition.py` (I-002 節のみ + import 1 行)、
`tests/test_paper01_scope/test_sparsify.py` (新規)、
`experiments/20260908-paper01-scope-condition/results/mutation-log-I002.md`
(新規) の 3 つ。`design-final.md` は未変更。`paper01_external_audit.py`
を含む凍結 apparatus には一切触れていない。既存 6 dataclass 契約・
I-001/I-001b のロジックも無改変 (git diff は追記のみ、94 insertions,
0 deletions)。

**残課題**:
- fidelity pin (b) (セル全体 dedupe→cap→draw が ARM-A と一致すること) は
  本 issue の scope 外、I-006 の責務 (issue Background/design-final.md §3
  Stage 2 のとおり)。
- `InternalReference` の穴埋めは I-006 の責務 (本 issue は低水準 ρ 関数
  のみ、Codex TASK-PRE HIGH-5 反映済)。
- `radius` 単調性が全域定理でないという発見 (乱数 20000 試行で ~1% 非単調)
  は I-006 の τ* 較正ロジックにも波及しうる (τ ラダー全段で必ずしも
  滑らかに単調とは限らない可能性)。design-final.md §2.4 は「単調性は
  支持補強であって判定には使わない」と既に明記しているため設計上は
  吸収済みだが、**I-006 実装時にこの非単調性の実例を踏まえて念のため
  再確認することを推奨** (blockers.md への記録は本 issue の scope 外
  なので、この Execution Result に書き残すのみ)。
