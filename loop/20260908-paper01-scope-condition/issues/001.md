# Issue 001: 実験雛形 + 事前登録の凍結 (`Final` 定数 + `config.json` の二重固定)

verify_level: recheck

## Goal

`design-final.md` §9 の凍結値を **`Final` 定数と `config.json` で二重固定**し、
実験ディレクトリ `experiments/20260908-paper01-scope-condition/` の骨格を作る。
**以降のすべての issue がこの定数を import する**。

## Background

G1 では事前登録を I-002 で凍結し、「結果が期待と逆でも `design-final` を開けなかった」
のはこの二重固定が効いたため。本タスクは **B-G1-10 の判断規則が凍結済**で、
**測る前に条件と判定規則を凍結する**ことが binding。**定数がコードと config で
食い違ったまま走ると、事前登録の意味が消える**。

`reproducibility-discipline` Skill を Read してから着手すること。

## Scope

### In
- `scripts/paper01_scope_condition.py` の **冒頭 (paths / constants / preregistration) 節のみ**
  - 二経路 import helper は `paper01_external_audit.resolve_sibling_script` を**再利用**する
  - `_c` (`es4_actuator.constants`) と `_g1` (`paper01_external_audit`) から
    **import して再定義しない**値: `AUC_FLOOR` / `REF_DEDUP` / `N_R_MIN` / `N_R_MAX` /
    `CI_ALPHA` / `N_RESAMPLES` / `MIN_CLASS_N` / `ANCHOR_DRAWS`
  - 本 ADR 固有の `Final` 定数: `TAU_GRID` / `STAGE1_REPLICATES`(500) /
    `STAGE1_DRAW_INDEX`(0) / `STAGE1_INTERNAL_GOOD_COUNTS` /
    `STAGE1_INTERNAL_DROP_REF`(0.2350) / `DELTA_TS_CANDIDATES` /
    `DELTA_MATERIAL_EPS`(0.01) / `SEED` / `FAIL_PRECEDENCE`(F4,F1,F2,F3,F5,F6,F7)
  - `collect_preregistration()` / `write_config()` / `load_config()` /
    `config_matches_preregistration()`
- **下流 issue が共有する結果 dataclass の「契約」をここで先に凍結する**
  (フィールド名と型のみ。ロジックは各 issue が入れる):
  `DeltaDecomposition` (I-003) / `Stage1Result` (I-004) / `CellResult` (I-006) /
  `InternalReference` (I-006) / `TauCalibration` (I-006) / `ScopeVerdict` (I-005)
  — **I-002..I-005 を並列実行するため、インタフェースを先に固定しないと合流時に食い違う**
- `experiments/20260908-paper01-scope-condition/`: `SEED` / `config.json` / `env.md` / `data.md`
  (`data.md` は **G1 の入力を再利用する**旨と SHA-256 pin を明記。新規取得はしない)

### Out
- 疎化 / Δ 分解 / Stage 1 / 判定 / 配線 (I-002 以降)
- `scripts/paper01_external_audit.py` の**編集** (import のみ)
- 新規コーパスの取得

## Allowed Files
- `scripts/paper01_scope_condition.py` (新規)
- `experiments/20260908-paper01-scope-condition/{SEED,config.json,env.md,data.md}` (新規)
- `tests/test_paper01_scope/{__init__.py,test_preregistration.py}` (新規)

## Acceptance Criteria

| AC | 条件 | test |
|---|---|---|
| 001-1 | 共有定数は `es4_actuator.constants` / `paper01_external_audit` から **import され再定義されていない** | `test_shared_constants_are_imported_not_redefined` |
| 001-2 | `config.json` の全キーが `collect_preregistration()` と一致 | `test_config_matches_preregistration` |
| 001-3 | **恒真でない** — `monkeypatch` で定数を 1 つ変えると 001-2 が**落ちる** | `test_config_mismatch_is_detected` |
| 001-4 | `SEED` ファイルの中身が `SEED` 定数と一致 | `test_seed_file_matches_constant` |
| 001-5 | `TAU_GRID` は降順・重複なし・全要素が `(0,1)` | `test_tau_grid_is_well_formed` |
| 001-6 | `STAGE1_INTERNAL_GOOD_COUNTS` の合計 = 25、長さ = 16 (内部 gold の実測形状) | `test_stage1_internal_shape_matches_sealed_gold` |
| 001-7 | `DELTA_TS_CANDIDATES` == `("X0","X3a","X3b","X3c","C0","C4")` (純 max-cos のみ) | `test_delta_ts_candidates_are_pure_max_cos` |
| 001-8 | `FAIL_PRECEDENCE` が F1-F7 を**過不足なく**含み、先頭が `F4` | `test_fail_precedence_is_complete_and_ordered` |
| 001-9 | 共有 dataclass の**契約**が凍結されている — `DeltaDecomposition`/`Stage1Result`/`CellResult`/`InternalReference`/`TauCalibration`/`ScopeVerdict` のフィールド名・型が定義済みで、下流 issue が import できる | `test_shared_result_contracts_are_frozen` |
| **001-MUT** | 上記の境界述語を機械的に総当たりして全 kill。`assert old in s` / `assert new != old` の二重確認つき | `results/mutation-log.md` |

## Test Plan
- **encoder 完全非依存**。`[eval]` extras 不要。
- 001-6 は**封印済み gold を読んで**形状を assert する (`_diag.load_adversarial_labeled()`)。
  ハードコードした期待値と実データの**両方**を突き合わせる (片側だけだと恒真になる)。
- 001-1 は AST ではなく **`is` 同一性 / 値一致 + `monkeypatch.setattr` で追随すること**を assert。

## Stop Conditions
- 共有定数を再定義しないと成立しない設計だと判明 → **停止して ADR へ差し戻し**
- `paper01_external_audit.py` / 凍結 apparatus の編集が要る → **S3**
- 新規依存が要る → **S4**

## Dependencies
- なし (**最初の issue**)

## Status
done

## Execution Result

**実装 (attempt 1、追加の手戻りなし)**:

- `scripts/paper01_scope_condition.py` (新規) - 冒頭 (paths/constants/preregistration)
  節のみ。二経路 import bootstrap で `paper01_external_audit` (`_g1`) を import し、
  `_g1.resolve_sibling_script` を再利用して `es4_scorer_diag` (`_diag`) を解決
  (新しい helper は書いていない)。共有定数 8 個 (`AUC_FLOOR`/`REF_DEDUP`/`N_R_MIN`/
  `N_R_MAX` は `_c` = `es4_actuator.constants` から、`CI_ALPHA`/`N_RESAMPLES` も `_c`
  から、`MIN_CLASS_N`/`ANCHOR_DRAWS` は `_g1` から) は import のみで module 冒頭に
  literal 再定義していない。本 ADR 固有の `Final` 定数 (`TAU_GRID` / `STAGE1_REPLICATES`
  =500 / `STAGE1_DRAW_INDEX`=0 / `STAGE1_INTERNAL_GOOD_COUNTS` / `STAGE1_INTERNAL_DROP_REF`
  =0.2350 / `DELTA_TS_CANDIDATES` / `DELTA_MATERIAL_EPS`=0.01 / `SEED`=20260908 /
  `FAIL_PRECEDENCE`) は design-final.md §9 の値をそのまま transcribe。
  `collect_preregistration()`/`config_matches_preregistration()`/`write_config()`/
  `load_config()`/`write_seed_file()`/`load_seed_file()`/`write_env_md()` は G1 の
  I-002 と同型 (非キャッシュ・read-through)。
  共有結果 dataclass 6 種 (`DeltaDecomposition`/`Stage1Result`/`CellResult`/
  `InternalReference`/`TauCalibration`/`ScopeVerdict`) をフィールド名+型のみの
  frozen dataclass として契約凍結 (ロジック無し、下流 issue が import する)。
- `experiments/20260908-paper01-scope-condition/{SEED,config.json,env.md,data.md}`
  (新規、`write_seed_file()`/`write_config()`/`write_env_md()` で生成 + `data.md` は
  G1 の入力再利用と SHA-256 pin を明記する手書き)。
- `tests/test_paper01_scope/{__init__.py,test_preregistration.py}` (新規、9 test、
  AC 001-1〜001-9 に 1:1 対応)。

**test (9/9 green)**: `test_shared_constants_are_imported_not_redefined` (001-1、
monkeypatch 追随を `_c`/`_g1` 両方で確認、AST 不使用 = design-final §7 Test Plan
指示通り) / `test_config_matches_preregistration` (001-2) /
`test_config_mismatch_is_detected` (001-3、恒真でないことを monkeypatch で証明) /
`test_seed_file_matches_constant` (001-4) / `test_tau_grid_is_well_formed` (001-5) /
`test_stage1_internal_shape_matches_sealed_gold` (001-6、
`es4_scorer_diag.load_adversarial_labeled()` + `load_aut_battery()` を実際に読み、
`good` カテゴリを AUT 16 物体で集計した実測 multiset と突き合わせ - ハードコード
期待値だけの恒真 assert ではない) / `test_delta_ts_candidates_are_pure_max_cos`
(001-7) / `test_fail_precedence_is_complete_and_ordered` (001-8) /
`test_shared_result_contracts_are_frozen` (001-9、6 dataclass のフィールド名+型を
exact match + `FrozenInstanceError` を実際に発火させて確認)。

**mutation (AC 001-MUT、全 19 mutant KILL・生存 0)**: 凍結定数の値/順序/構成を壊す
mutant 12 件、read-through 配線自体を壊す mutant 4 件 (うち `config_matches_
preregistration` を恒真化する M13 は「001-2 は生存するが 001-3 が単独で kill する」
非対称性を実測で確認 - G1 の反省 (二重確認なしでは恒真検査を素通りする) が
本タスクでも効くことを裏取りした)、dataclass 契約の frozen/フィールド名を壊す
mutant 2 件。各 mutant で `assert old in s` と `assert new != old` の二重確認 +
書込み後の `assert new in on_disk` を実施、復元後は byte 一致を assert (全件 pass)。
mutation 後に `config.json` を実読みして数字の意味を design-final.md §9 と 1 対 1
突き合わせ (詳細: `experiments/20260908-paper01-scope-condition/results/mutation-log.md`)。

**検証コマンド (すべて exit 0)**:
- `pytest -q tests/test_paper01_scope` → exit 0 (9 passed)
- `mypy src` → exit 0 (247 source files, no issues)
- `ruff check src tests` (実 CI parity スコープ、`scripts/dev/pre-push-check.*` と同一)
  → exit 0 / 文字通りの `ruff check .` は repo 全体スキャンで **本タスク無関係の
  既存違反 197 件** (`scripts/policy_banned_imports.py` 等、Allowed Files 外、
  main HEAD 時点で既に違反 = pre-existing) を検出するが、自分の新規ファイルは
  0 件ヒット (grep で確認済)
- `ruff format --check src tests` → exit 0 / 文字通りの `ruff format --check .`
  は同じ理由で既存 5 ファイル (Allowed Files 外) の reformat 要求を検出するが
  自分の新規ファイルは対象外

**残課題 / 申し送り**:
- I-002 以降が `scripts/paper01_scope_condition.py` に追記していく前提
  (Scope In の通り、本 issue はモジュール冒頭節のみ)。
- `.venv` はこの worktree に存在しなかったため、main checkout の
  `C:\ERRE-Sand_Box\.venv\Scripts\python.exe` を絶対パスで使用 (新規依存追加なし、
  同一 lockfile を共有)。次 issue の executor も同様の対応が必要な可能性がある。
- `results/mutation-log.md` は issue の Allowed Files 表には明記が無いが、
  task prompt 本文が明示的に要求する成果物のため `experiments/20260908-
  paper01-scope-condition/` 配下 (Allowed Files が新規作成を許可しているディレクトリ)
  に作成した。
- 全体 (repo 全ファイル) の `pytest -q --ignore=tests/test_godot` はバックグラウンドで
  起動したが本 issue の検証時間内に完走しなかった (スコープ外、`pytest -q
  tests/test_paper01_scope` のみが D1 の要求)。
