# Issue 004: Stage 1 — deflation 検定 (内部形状への縮小抽出と drop 帯)

verify_level: recheck

## Goal

`design-final.md` §3 Stage 1 を実装する。外部 gold を**内部と同じ形**へ縮小抽出し、
`drop_r` の 5-95 パーセンタイル帯が**内部 C0 pooled drop 0.2350 を含むか**を判定する。

## Background

**これは最も deflating な対立仮説 (「内部の崩落は 41 項目・有効ペア 25 の小標本効果」)
を先に潰す工程であり、判定順序で必ず最初に評価される。**
**この検定が「支持」に倒れると論文 01 は書かない**ので、
**書きたい方向へ倒れやすい設計になっていないか**を特に厳しく見ること。

**Codex HIGH-5 (反映済)**: anchor は **`STAGE1_DRAW_INDEX = 0`** に固定する。
当初案の「median-by-drop draw」は **drop (= outcome) で選ばれた draw** であり、
**外部の outcome で anchor を選んでから小標本検定する** forking-paths 構造だった。
**median draw は sensitivity として併記するのみ。**

## Scope

### In
- `internal_shaped_replicates(contexts, *, replicates, corpus, scheme, seed) -> list[GoldSubset]`
  - 物体あたり **common 1 件**
  - good は `STAGE1_INTERNAL_GOOD_COUNTS` を **`object_weight` 降順の active 物体へ巡回割当**
  - seeded 復元なし抽出。`derive_seed("stage1", corpus, scheme, replicate_index)`
  - **Codex TASK-PRE MEDIUM-2 反映**: `corpus` / `scheme` を**明示引数**にする。
    module global や暗黙の文脈から**推測して埋めることを禁止**する
    (seed が silently 変わると replicate が再現しない)
  - プールが足りない物体の扱いを**明示凍結**する (件数を surface、無音で減らさない)。
    → `Stage1Result.n_objects_pool_short` / `.pool_short_objects` に**必ず出す**
- `stage1_drop_distribution(draw_items, subsets) -> Stage1Result`
  - **pooled AUC を主** (内部と同じ統計量)、**層別を副**として両方
    → 副は `drop_p5_strat` / `drop_p95_strat` / `median_drop_strat` に入れる
  - `drop_r` の 5-95 パーセンタイル / 中央値 / eligible replicate 割合
  - **eligible 限定帯** (`design-final.md:165` の「副」) を
    `drop_p5_eligible` / `drop_p95_eligible` / `median_drop_eligible` に入れる
  - **median-by-drop draw の sensitivity** (DA-SC-12) を
    `median_draw_drop` / `median_draw_outcome` に併記する。**主判定には使わない**
- `stage1_outcome(band, reference_drop, eligible_fraction) -> str`
  — **`STAGE1_OUTCOME_ENUM` の 3 値を返す全域関数** (Codex TASK-PRE HIGH-3 / DA-SC-19、
  **本 issue の核**)。I-001b が契約を用意済:
  - `eligible_fraction <= 0.5` → **`"INCONCLUSIVE"`** (`design-final.md:165` の「判定不能」)
  - 帯が `STAGE1_INTERNAL_DROP_REF` (0.2350) を含む → `"DEFLATION_SUPPORTED"`
  - それ以外 → `"DEFLATION_REJECTED"`
  - **`"INCONCLUSIVE"` を `"DEFLATION_REJECTED"` に丸めてはならない**。
    丸めると `design-final.md:251` の出口表で**「書く」方向へ倒れる**
- `stage1_deflation_supported(...) -> bool` は
  **`stage1_outcome(...) == "DEFLATION_SUPPORTED"` の派生ミラー**として実装する
  (独立に条件を書き直さない)

### Out
- τ\* 較正 / セル配線 (I-006) / 判定統合 (I-005)
- 実コーパスでの実走 (I-007)

## Allowed Files
- `scripts/paper01_scope_condition.py` (**Stage 1 の節のみ**)
- `tests/test_paper01_scope/test_stage1.py` (新規)
- `experiments/20260908-paper01-scope-condition/results/mutation-log-I004.md` (新規、MEDIUM-5)

## Acceptance Criteria

| AC | 条件 | test |
|---|---|---|
| 004-1 | 抽出形状 — 全 replicate で **物体あたり common ちょうど 1 件**、good 合計が割当どおり | `test_replicate_shape_matches_internal` |
| 004-2 | pooled ペア数の同一物体割合が内部 (6.25%) と**同オーダー** | `test_same_object_pair_share_is_internal_scale` |
| 004-3 | **決定論** — 同じ seed で同じ抽出、**別 seed で別の抽出** | `test_replicates_are_seeded_and_differ_across_seeds` |
| 004-4 | **anchor は `draw_index=0`** に固定されている (outcome 非依存) — spy で実引数を確認 | `test_stage1_uses_draw_index_zero_not_median` |
| 004-5 | median-by-drop draw は **sensitivity としてのみ**出力に現れ、主判定に**入らない** | `test_median_draw_is_sensitivity_only` |
| 004-6 | 縮小以外が動いていない — 同じ anchor・同じ候補で、**全項目を渡すと G1 の pooled 値に一致** | `test_full_gold_reproduces_g1_pooled` |
| 004-7 | 帯の判定が**非恒真** — 帯が `0.2350` を含む fixture と含まない fixture の**両方**で正しく分岐 | `test_deflation_band_both_branches` |
| 004-8 | eligible 過半割れ — X0 が過半で ineligible のとき **「判定不能」**として記録され、eligible 限定の帯を**副**として出す | `test_ineligible_majority_is_recorded_as_undecidable` |
| 004-9 | プール不足の物体が**無音で消えない** (件数つきで surface) | `test_insufficient_pool_objects_are_surfaced` |
| **004-MUT** | 境界述語 (パーセンタイル境界の `<`/`<=`、`draw_index` の定数、巡回割当の開始位置、過半の `>` vs `>=`、pooled/層別の取り違え) を**機械的に総当たり**して全 kill | `results/mutation-log.md` |

## Test Plan
- **encoder 非依存** — 合成 `DrawItems` (scores/labels/objects を手組み) で全経路を通す。
- 004-6 のみ実データ経路を使うが、**G1 の既報値と突き合わせる**形にして恒真を避ける。
- 004-4 / 004-5 は **spy で `anchor_draw_indices` の実引数 `draw_index` を見る**。
  **AUC などの集約統計で配線を検査しない** (G1 の ARM-X で kill できなかった失敗の再発防止)。
- 004-7 は **両分岐に到達する fixture** を置く。

## Stop Conditions
- 内部形状に合わせると Ocsai の active 物体が足りず抽出が定義できない → 停止して ADR へ差し戻し
- `paper01_external_audit.py` の編集が要る → **S3**

## Dependencies
- I-001
- **I-001b (共有契約の穴埋め + 節 anchor)** — Codex TASK-PRE HIGH-3/4/5 + MEDIUM-1 反映。
  **これの完了前に着手しない** (契約が変わるため)

## TASK-PRE 反映 (Codex `_codex-preplan.md`、Verdict Revise)

### 追記位置 (MEDIUM-1、**衝突回避のため binding**)

`scripts/paper01_scope_condition.py` には I-001b が節 anchor を置いてある。
本 issue のコードは **自分の anchor 直下にのみ**追記する:

```python
# ---------------------------------------------------------------------------
# I-004: Stage 1 -- deflation test
# ---------------------------------------------------------------------------
```

**ファイル末尾 (EOF) に追記しない。** 4 branch が同じ EOF を触ると merge conflict になる。

### mutation log の分離 (MEDIUM-5)

本 issue の mutation log は **`experiments/20260908-paper01-scope-condition/results/mutation-log-I004.md`**
に書く (共有の `mutation-log.md` に直接書かない)。統合は I-007 が行う。

### 追加で必須の境界 test (Codex TASK-PRE MEDIUM-3)

- `STAGE1_INTERNAL_DROP_REF` (0.2350) が **`drop_p5` / `drop_p95` にちょうど一致**する場合
  (帯は閉区間か開区間か。**含む = `"DEFLATION_SUPPORTED"`** に凍結し test で pin)
- **`eligible_fraction == 0.5` ちょうど** (`<= 0.5` が `"INCONCLUSIVE"` なので**該当する**)
- **一部の物体だけ** pool 不足のケース (全物体不足ではない。`n_objects_pool_short` が
  0 でも全数でもない値を取り、**無音で減っていない**ことを確認)


## Status
done

## Execution Result

**base 検証**: worktree HEAD が stale (`9a13b6c`) だったため、指示どおり
`git reset --hard 3ab8549` (Merge I-005) を実行してから着手 (作業ツリーは
clean だったため discard は安全)。

**実装** (`scripts/paper01_scope_condition.py` の `# I-004: Stage 1 --
deflation test` anchor 直下、EOF 追記なし):

- `ObjectGoldPool` / `GoldSubset` — 本 issue 固有の契約 (I-001b の 6 dataclass
  とは別)。`item_indices` は呼び出し元が共有する item 配列への index。
- `internal_shaped_replicates(contexts, *, replicates, corpus, scheme, seed)`
  — 物体あたり common ちょうど 1 件 + `STAGE1_INTERNAL_GOOD_COUNTS` を
  `object_weight` 降順の active 物体へ巡回割当した件数の good を、
  `_g1.anchor_draw_indices` で seeded 復元なし抽出。**anchor は
  `STAGE1_DRAW_INDEX`(=0) を文字通り毎回渡す** (`seed` 側だけが
  replicate/物体/good-or-common で変わる) — DA-SC-12 の
  outcome 非依存性を満たす。プール不足の物体は best-effort で抽出しつつ
  `pool_short_objects` に必ず surface (無音で減らさない)。
  `seed` 引数は `_g1.derive_seed` の**先頭 part として自前で畳み込み**、
  `corpus`/`scheme` とともに明示引数として load-bearing にした
  (Codex TASK-PRE MEDIUM-2)。
- `same_object_pair_share` — AC 004-2 用の診断関数 (pooled pair のうち
  同一物体由来の割合)。
- `_stage1_auc_unweighted_by_object` — 層別 (副) 統計、`_g1.controls.auc`
  を物体ごとに呼び重み付けせず平均 (pooled=主 とは別コード経路)。
- `stage1_outcome(band, reference_drop, eligible_fraction) -> str` —
  **全域関数、3 分岐**: `eligible_fraction <= 0.5` → `"INCONCLUSIVE"`
  (最優先・無条件) / 帯が `reference_drop` を**閉区間**で含む →
  `"DEFLATION_SUPPORTED"` / それ以外 → `"DEFLATION_REJECTED"`。
  `"INCONCLUSIVE"` を `"DEFLATION_REJECTED"` に丸める分岐は存在しない。
- `stage1_deflation_supported` — `stage1_outcome(...) ==
  "DEFLATION_SUPPORTED"` の派生ミラーとして実装 (独立に条件を書き直さない)。
- `stage1_drop_distribution(draw_items, subsets, *, candidate,
  reference_drop=...) -> Stage1Result` — R replicate を集約し、pooled
  (`_g1.auc_stratified`) と層別を両方計算、`Stage1Result` の全フィールド
  (`drop_p5_eligible` 等の副次帯・`n_objects_pool_short`・
  `median_draw_drop`/`median_draw_outcome` sensitivity 含む) を埋める。
  median-by-drop draw は `_g1.median_draw_index` (凍結) で選び、**主判定
  (`drop_p5`/`drop_p95`/`stage1_outcome`) には一切混ざらない**。

**テスト** (`tests/test_paper01_scope/test_stage1.py`、新規 24 本):
AC 004-1〜004-9 それぞれに専用 test 1 本以上 + Codex TASK-PRE MEDIUM-3 の
境界 test 3 本 (`0.2350` が帯端にちょうど一致/`eligible_fraction==0.5`
ちょうど/一部物体のみ pool 不足) + 004-MUT の境界 mutant を狙い撃ちする
witness test 3 本 + pooled/層別が実際に乖離することを示す test 1 本。
encoder 非依存 (合成 `DrawItems`/`ObjectGoldPool` のみ)。004-6 のみ
「全項目を渡すと縮小コードが何もしていないこと」を、G1 の凍結
`auc_stratified` を独立オラクルとして直接呼んだ値と突き合わせて検証
(tie を含む fixture で naive reimplementation との乖離も検出できる形)。

**4 つの verify (foreground、pin 済みコマンド、main checkout
`.venv/Scripts/python.exe` 絶対パス使用)**:

| コマンド | exit code | 結果 |
|---|---|---|
| `pytest -q tests/test_paper01_scope` | 0 | 59 passed |
| `mypy src` | 0 | 247 files, Success |
| `ruff check src tests scripts/paper01_scope_condition.py` | 0 | All checks passed |
| `ruff format --check src tests scripts/paper01_scope_condition.py` | 0 | 598 files already formatted |

**mutation testing (`results/mutation-log-I004.md`)**: 7 mutant、**7/7 KILLED
(生存 0)**。issue 本文が名指しした 3 つの核心 mutant
(`eligible_fraction <=`→`<` 緩和 / `INCONCLUSIVE`→`DEFLATION_REJECTED` 丸め
/ 帯の閉区間→開区間) は**すべて専用の境界 pin test で個別に kill**を確認
(他 test の巻き添えでなく、そのものの assertion が落ちた)。加えて
`draw_index` 定数変更・巡回割当の開始位置ずれ・pooled/層別の取り違え・
`DEFLATION_SUPPORTED`/`DEFLATION_REJECTED` 入れ替えの計 4 mutant も追加で
kill を確認。ファイル復元は毎回 SHA-256 で pristine
(`5bf93ff4...a259c1a`) と一致することを確認。

**mutation harness の gotcha (honest 記録)**: mutant 適用→テスト→復元を
同一秒内に繰り返す driver 構成のため、Windows の mtime 粒度により
**stale な `__pycache__` バイトコードキャッシュ**が「復元済みの正しい
ソース」に対して古い (mutant 適用時の) コンパイル結果を返し続ける事故が
一度発生した (3 test が誤って fail)。`inspect.getsource` と直接の
SHA-256 比較で `.py` 自体は一度も壊れていないことを確認した上で、
`python -B` (bytecode 書き込み無効化) + `pytest -p no:cacheprovider` を
driver に追加して解消。以降のセッション内 verify はすべて
`__pycache__` を都度削除してから実行し、クリーンな結果のみを最終判定に
採用した。

**三状態の境界 mutant が kill されたか**: **されている** (M1: eligible
floor `<=`→`<`、M2: INCONCLUSIVE 丸め、M3: 帯の開区間化 -- いずれも
issue 本文が名指しした核心 mutant で、専用 test が個別に落ちることを
実地確認)。

**Allowed Files 逸脱**: なし。`scripts/paper01_scope_condition.py` (I-004
anchor のみ) / `tests/test_paper01_scope/test_stage1.py` (新規) /
`experiments/20260908-paper01-scope-condition/results/mutation-log-I004.md`
(新規) の 3 ファイルのみ変更。`design-final.md` は無改変。凍結
dataclass (`Stage1Result` 等) は import のみで再定義せず。

**残課題**: `verify_level: recheck` につき独立再実行 (loop-watchdog /
orchestrator) が未実施。フルスイート (`pytest -q --ignore=tests/test_godot`)
は DA-SC-20 により統合時まで延期 (本 issue 単体では
`tests/test_paper01_scope` scoped run のみ)。
