# Issue 002: cognition/embodiment.py 履歴依存目的地幾何 + policy grammar freeze + continuity control
verify_level: recheck   # 器官の中核 (memory→geometry)・policy grammar 契約・最重要 Codex 付託点 (continuity)

## Goal
LLM が選んだ zone 内の *where* を agent 自身の history から決める resolver を
`cognition/embodiment.py` に実装する。allowed history features を閉列挙し LLM が座標を出せない
ことを test で凍結、continuity gate (exact oracle causal wiring、measurement 非再入) を
positive/negative control で assert する。`EclRecordMode` dataclass もここに定義する
(cycle I3 が消費・integration I4 が構築する cognition 層契約)。

## Background
design-final §論点2 (policy grammar freeze) + §論点4 (continuity gate) + DA-ECLIMPL-2/3/4 +
Codex HIGH-2 (k_world=0) / HIGH-3 (exact oracle)。resolver は RankedMemory の x/z 算術のみ =
cognition が memory/schemas/contracts を合法 import。frozen `running/policy.py` `_forage_centroid`
/ `_reflect_clamp` / `_disc_jitter` を写経。**設計 FROZEN。**

## Scope
### In
- `cognition/embodiment.py` 新規:
  - `EclRecordMode` dataclass = **軽量 config のみ** (fixed now_factory / named RNG substream factory /
    deterministic id·ts base / K_ECL=8 / reflection_disabled flag / run_id / callback)。**trace/manifest/
    checksum 型は含めない** (それらは integration 層、Codex TASK-PRE MEDIUM: 層汚染回避)
  - `resolve_destination(...)` = agent 自身の retriever で `retrieve(k_agent=K_ECL, k_world=0,
    current_location, mark_recalled=False)` → strength-weighted centroid (tie-break 全順序
    `(-strength, created_at, id)`) → `disc_jitter(micro substream)` を加算 → `reflect_clamp`
    (frozen policy.py L421-436 の **centroid+jitter then clamp** 順序に厳密一致)。空 retrieval /
    located なし / total_weight<=0 fallback = `default_spawn(zone)`。centroid/provenance(memory_ids)/
    jitter/pre_clamp/post_clamp/clamp_fired を決定記録として返す (absolute target を replay に使わない)
  - docstring に allowed history features 閉列挙 + "NOT a structural-floor verdict; verdict は holding"
### Out
- cycle への配線 (I3) / world sink (I3) / record-replay driver (I4)
- 統計/floor/bootstrap/landscape・SPDM ladder/constants の import・計算・出力 (禁止、holding)
- variant (no-reflect/uniform/top-1) の live 露出 (frozen §4.5、forensic control のみ)

## Allowed Files
- `src/erre_sandbox/cognition/embodiment.py` (新規)
- `tests/test_cognition/test_ecl_embodiment.py` (新規)

## Acceptance Criteria (AC↔test マッピング)
- AC1 policy grammar 凍結: `test_ecl_embodiment.py::test_ecl_v0_policy_grammar_frozen` 緑
  — LLM が不正座標/重みを注入しても resolver は centroid 経路のみ通る + fake retriever で `k_world=0`
  を渡し world-scope memory が centroid に入らない negative case (Codex HIGH-2)
- AC2 continuity positive (exact oracle、clamp 両分岐): `test_ecl_v0_continuity_positive_control` 緑
  — memory を既知座標 L(zone Z、`L != default_spawn(Z)`) に seed + fixed candidate + pinned jitter substream
  で 1 resolve → `target == reflect_clamp(strength_weighted_centroid(L) + fixed_jitter, Z)` の**決定的等価**
  (ε 非使用) かつ `target != default_spawn(Z)`。**pre_clamp が zone 外 (clamp 発火) / zone 内 (非発火) の
  両ケースを別々に pin** (Codex TASK-PRE MEDIUM)。決定記録を transform 再計算で照合 (ablation が効く)
- AC3 continuity negative + fallback: `test_ecl_v0_continuity_negative_control` 緑
  — retriever 空返し (memory→geometry edge 切断) → `target == default_spawn(Z)` かつ positive の target と
  明確に相異。**`location=None` / `total_weight<=0` の fallback も別 test で pin** (Codex TASK-PRE MEDIUM)
- AC4 history 依存: `test_ecl_v0_move_msg_history_dependent` 緑 — resolved target ≠ default_spawn(Z)
- AC5 measurement 非再入 (import/output guard、語句 ban でない): `test_no_measurement_reentry` 緑
  — embodiment.py が `erre_sandbox.evidence`/`spdm`/`runningness` を import しない + verdict/floor/landscape
  の**数値 output を出さない** (Codex TASK-PRE LOW: "NOT a structural-floor verdict" docstring を
  false-positive しないよう語句 ban でなく import/output guard に寄せる)
- AC6 CI parity: `bash scripts/dev/pre-push-check.sh` 4 段全 pass

## Test Plan
`pytest -q tests/test_cognition/test_ecl_embodiment.py` (test-standards 準拠: sqlite-vec `:memory:`、
LLM モック不要 — resolver は memory のみ読む純関数) + pre-push CI parity 4 段。

## Stop Conditions
- 全 AC 緑 (Done)
- exact oracle が frozen policy と一致せず契約矛盾が判明 (Stop、superseding ADR 要)
- tune-to-pass が必要になった時点で停止 (契約を緩めず escalate)
- budget 到達 (Stop)

## Dependencies
- **I1 完了必須** (`contracts/geometry` の CELL_MICRO_RADIUS_M/reflect_clamp/disc_jitter/ZONE_CENTERS/
  default_spawn を import)
- 写経元 (read-only): `evidence/d0_substrate/running/policy.py` L244-275/L221-241/L214-218
- import 再利用: `memory.retrieval.Retriever` / `memory.store` / `schemas`

## Status
DONE

## Execution Result
- **実装**: `src/erre_sandbox/cognition/embodiment.py` 新規 (K_ECL=8 / `SupportsRetrieve` Protocol /
  `EclDestination` 決定記録 / `EclRecordMode` 軽量 config / `strength_weighted_centroid` helper /
  `resolve_destination` async 純関数)。frozen `policy.py` L421-436 の centroid+jitter **THEN** clamp 順序に
  厳密一致。I1 の `contracts.geometry.disc_jitter/reflect_clamp/default_spawn/locate_zone` を import 再利用
  (再写経なし)。EclRecordMode は trace/manifest/checksum 型を含めず (Codex TASK-PRE MEDIUM: 層汚染回避)。
- **test**: `tests/test_cognition/test_ecl_embodiment.py` 新規、17 tests 緑。
  - AC1 `test_ecl_v0_policy_grammar_frozen`: signature freeze (LLM = destination_zone:Zone のみ・座標注入不可)
    + spy retriever で `k_agent=K_ECL, k_world=0, mark_recalled=False` binding 検証 + world-scope memory が
    centroid/provenance に入らない negative (Codex HIGH-2)。
  - AC2 `test_ecl_v0_continuity_positive_control_clamp_{not_fired,fired}`: exact oracle (ε 非使用、
    centroid=w·p/w bit-exact 再導出 + transform 再計算照合)、clamp 発火 (memory@GARDEN→zone STUDY) /
    非発火 (memory@STUDY内) 両分岐分離 (Codex TASK-PRE MEDIUM)。
  - AC3 `test_ecl_v0_continuity_negative_control` (空 retrieval→default_spawn、ablation で positive と相異) +
    `test_ecl_v0_fallback_no_located_location_none` / `test_ecl_v0_fallback_total_weight_non_positive` 別 pin。
  - AC4 `test_ecl_v0_move_msg_history_dependent`: resolved target ≠ default_spawn。
  - AC5 `test_no_measurement_imports` / `test_no_measurement_output_identifiers`: AST import guard
    (evidence/spdm/runningness 不 import) + identifier-level output guard (floor/landscape/verdict は
    docstring 許容・語句 ban でない、Codex TASK-PRE LOW)。
  - 補助: `strength_weighted_centroid` exact math + `EclRecordMode` determinism handles。
- **AC6 CI parity**: pre-push 4 段 **ALL CHECKS PASSED** (ruff format --check / ruff check / mypy 229 files /
  pytest 3340 passed, 66 skipped, 374s)。
- **契約矛盾なし** (frozen policy と exact oracle 一致、tune-to-pass 不要)。measurement 非再入 (verdict holding)。
- commit: feat/ecl-v0 (次 commit)。後続 = I3 (ECL live seam foundation) は fresh session 推奨。
