# Issue 003: ECL live seam foundation (cycle 分岐 + world sink + 決定性注入点 + flag-off byte invariant)
verify_level: recheck   # live hot path 改変・byte-invariant 契約・I4 harness が依存する全 seam を所有

## Goal
ECL 器官が必要とする **cycle.py + world/tick.py の live seam を全て I3 が所有**する
(Codex TASK-PRE HIGH-1/HIGH-2 反映)。flag-on 時のみ: (1) 履歴依存座標を MoveMsg.target に埋める、
(2) reflection を skip、(3) memory id·ts·envelope sent_at を注入 factory で決定化、(4) ECL retrieval を
k_world=0/mark_recalled=False + resolver へ retriever handle 供給、(5) memory formation location を
現在位置で書く、(6) `ecl_trace_sink` を physics-tick 後に発火、(7) resolver の decision provenance を
CycleResult に surface。flag-off で全 envelope が byte 同一であることを保証する。I4 (integration 純 harness)
は本 seam を無改変で駆動できる状態にする。

## Background
design-final §論点1 (ECL 分岐 + world sink) + §論点3 (Plane 1 pin) + 基本方針 (v2 spine) +
**Codex TASK-PRE HIGH-1** (reflection disable/deterministic sent_at·id は integration 単独で完結不可、
現行 cycle.py が reflection 直呼び・uuid4/now で memory 書き) + **HIGH-2** (現行 `_write_observations` が
`MemoryEntry.location` 未設定ゆえ resolver の centroid が空間信号を持てない)。cognition が resolve した
具体座標 MoveMsg は `world/tick.py` L1692 の default_spawn resolution 分岐を発火させず world 無改変。
sink は既存 `IndividualTraceSink` 等の house-style (primitive + schemas 引数、closure が row 組み立て)。
**設計 FROZEN。**

## Scope
### In (cycle.py)
- `CognitionCycle.__init__` に `ecl_mode: EclRecordMode | None = None` 追加 (None=現行 byte 不変)
- `_build_envelopes`: flag-on 時 MoveMsg.target を embodiment.resolve_destination 経由 (self._retriever を
  resolver に渡す)。flag-off = 現行 `position.model_copy(update={"zone": ...})` を byte 保存
- flag-on 時: reflection skip (`ecl_mode.reflection_disabled`)、memory id=`ecl-{agent_id}-{agent_tick:04d}` /
  ts=`_BASE_TS+agent_tick·1s` / envelope sent_at を `ecl_mode.now_factory` で pin、ECL retrieval を
  k_world=0/mark_recalled=False、memory formation location を現在位置で書く
- resolver の decision provenance (centroid/provenance/jitter/pre_clamp/post_clamp/resolved_from) を
  CycleResult に surface (I4 が agent_tick 軸で assemble)
### In (world/tick.py)
- `ecl_trace_sink: Callable[[str, int, float, float, float, float, float, Zone], None] | None = None` を
  `WorldRuntime.__init__` に追加 (house-style primitive 引数 = agent_id, physics_tick_index, x, y, z, yaw,
  pitch, zone)。`_on_physics_tick` の `step_kinematics` 後に発火 (None で no-op)。physics_tick_index(30Hz) と
  agent_tick(cognition) 分離、`order_slot`=log 生成時 `sorted(agent_id)` 決定計算
### Out
- record-replay driver 本体 / EclRecordMode 構築 / Plane2 assemble / checksum (I4、integration のみ)
- embodiment resolver 本体 (I2 完了前提)
- contracts/ecl.py (不要 — sink は primitive house-style)

## Allowed Files
- `src/erre_sandbox/cognition/cycle.py` (ECL seam 追加、flag-off byte 不変)
- `src/erre_sandbox/world/tick.py` (sink 1 本 + physics-tick 発火)
- `src/erre_sandbox/schemas.py` (CycleResult に decision provenance carrier 追加が要る場合のみ、最小)
- `tests/test_cognition/test_ecl_cycle.py` (新規)
- `tests/test_world/test_ecl_trace_sink.py` (新規)

## Acceptance Criteria (AC↔test マッピング)
- AC1 flag-off byte 不変 (全 envelope + 時刻 pin): `test_ecl_cycle.py::test_ecl_flag_off_byte_invariant` 緑
  — ecl_mode=None で `_build_envelopes` の**全 envelope list** (AgentUpdate/Speech/Move/Animation/
  ReasoningTrace) の canonical JSON bytes が現行実装と同一。sent_at 等は固定 now で pin し比較安定化
  (Codex TASK-PRE HIGH edge)
- AC2 連続 transit: `test_ecl_trace_sink.py::test_ecl_v0_embodied_move_continuous` 緑
  — ecl_mode 有効時 MoveMsg target が具体座標、step_kinematics が tick>1 連続 transit (default_spawn 分岐非発火)
- AC3 history 依存 move (統合): `test_ecl_trace_sink.py::test_ecl_v0_move_msg_history_dependent` 緑
  — cycle 経由 resolved target ≠ default_spawn(zone)
- AC4 決定性注入点: `test_ecl_cycle.py::test_ecl_deterministic_injection` 緑
  — ECL mode で memory id=`ecl-{agent_id}-{tick:04d}` / ts が tick 由来 / sent_at が now_factory 由来、
  ECL retrieval が k_world=0 かつ recall_count 不変 (mark_recalled=False、Codex TASK-PRE MEDIUM) 、
  memory formation location が現在位置 (Codex HIGH-2)
- AC5 reflection skip: `test_ecl_cycle.py::test_ecl_reflection_skipped` 緑
  — ecl_mode.reflection_disabled で Reflector.maybe_reflect が呼ばれない (I4 の end-to-end pin の土台)
- AC6 sink no-op + 二軸: `test_ecl_trace_sink.py::test_ecl_trace_sink_noop_and_axes` 緑
  — sink=None で trace 非生成・現行不変、有効時 physics_tick_index/agent_tick 分離 + order_slot=sorted(agent_id)
- AC7 CI parity: `bash scripts/dev/pre-push-check.sh` 4 段全 pass

## Test Plan
`pytest -q tests/test_cognition/test_ecl_cycle.py tests/test_world/test_ecl_trace_sink.py` +
既存 cycle/tick 回帰全緑 (flag-off 不変) + pre-push CI parity 4 段。flag-off byte-invariant は既存
individual_trace_sink 等の flag-off 不変パターン踏襲。

## Stop Conditions
- 全 AC 緑 (Done)
- flag-off byte-invariant が破れる (Stop、現行挙動を変えてはならない)
- schemas.py CycleResult carrier 追加が既存 schema を破壊 (Stop、最小 additive でなければ escalate)
- budget 到達 (Stop)

## Dependencies
- **I1 + I2 完了必須** (`cognition/embodiment.EclRecordMode` / `resolve_destination` / decision record 型 /
  `contracts/geometry`)
- 写経: `world/tick.py` 既存 `IndividualTraceSink` 群 (L88-167 型定義, L510-548 param, L1707-1723 発火) の
  house-style (primitive+schemas 引数、closure が row 組み立て、world は integration/evidence 非 import)
- 参照: `world/tick.py` L1680-1697 (default_spawn resolution — 非発火を確認), L1125-1142 (_on_physics_tick),
  `cognition/cycle.py` reflection 呼び (L1119-1132) / memory 書き / retrieval 呼び

## Status
DONE

## Execution Result
**DONE** (2026-07-05, feat/ecl-v0, in-session 逐次)。AC1-7 全緑。

### 実装
- `cognition/cycle.py`: `__init__(ecl_mode: EclRecordMode | None = None)` (None で byte 不変)。
  Step 1 `_write_observations` に ECL 分岐 (deterministic id `ecl-{agent_id}-{tick:04d}` /
  tick 由来 `created_at` / 現在位置 formation `location` = Codex HIGH-2; flag-off は `location=None` で byte 同一)。
  move 決定は `_resolve_ecl_move` helper (flag-on 時のみ `embodiment.resolve_destination` 経由、move_decision_sink push)。
  `_build_envelopes(ecl_destination=...)` で MoveMsg.target 差し替え (flag-off = 現行 zone-only copy を byte 保存)。
  `_diagnose_and_reflect(reflection_disabled=...)` で record mode の reflection LLM 非発火 (Codex HIGH-1)。
  `_pin_envelope_clock` helper で envelope `sent_at` を fixed clock に pin (flag-off no-op)。
  step 複雑度維持のため ECL 2 ブロックを helper 抽出 (C901/PLR0912 回避)。
- `cognition/cycle.py` (`CycleResult` はここに定義): `CycleResult.ecl_destination: EclDestination | None = None`
  (最小 additive carrier) + success 経路で surface。schemas.py 変更は不要だった。
- `world/tick.py`: `ecl_trace_sink` 1 本 (house-style primitive 8 引数) + `physics_tick_index` (30Hz、cognition tick と別軸) +
  `_emit_ecl_trace` を `step_kinematics` 後・`sorted(agent_id)` 順に発火 (None で no-op、Codex MEDIUM-2/3)。

### テスト (新規 6、全緑)
- `tests/test_cognition/test_ecl_cycle.py`: `test_ecl_flag_off_byte_invariant` (AC1) /
  `test_ecl_deterministic_injection` (AC4) / `test_ecl_reflection_skipped` (AC5)。
- `tests/test_world/test_ecl_trace_sink.py`: `test_ecl_v0_embodied_move_continuous` (AC2) /
  `test_ecl_v0_move_msg_history_dependent` (AC3) / `test_ecl_trace_sink_noop_and_axes` (AC6)。

### 検証 (AC7)
- 新規 6 tests 緑 + 既存 test_cognition+test_world 618 全緑 (flag-off byte-invariant 実証)。
- CI parity 4 段緑: ruff format --check / ruff check / mypy src (229 files) / pytest (3346 passed, 66 skipped, 391s)。

### Out (未実施、後続 issue)
- record-replay driver 本体 / Plane2 assemble / checksum (I4)。handoff / Godot player (I5)。
