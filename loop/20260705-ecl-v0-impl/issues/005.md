# Issue 005: integration/embodied/handoff.py converter+manifest+golden + Godot EclReplayPlayer.gd
verify_level: recheck   # cross-machine handoff schema + committed golden (2 拠点非同期の契約面)

## Goal
G-GEAR (生成) → MacBook Godot (消費) を跨ぐ handoff artifact 群 (manifest.json / ecl_trace.jsonl /
decisions.jsonl / envelope_stream.jsonl / committed golden) を `integration/embodied/handoff.py` で
生成・検証し、Godot 側 dev-only `EclReplayPlayer.gd` で offline 再生 smoke まで完結する。主検証は
G-GEAR 側 wire-schema conformance (schema/golden test)。

## Background
design-final §論点5 + DA-ECLIMPL-5 + Codex HIGH-1 (FixturePlayer 無改変 claim 撤回、裏取り済) /
M3 (manifest checklist)。現行 `FixturePlayer.gd` は固定 DEFAULT_PLAYLIST のみで任意 stream 再生契約
なし。ゆえ Godot 側は dev-only harness の bounded 拡張 (本番 WS 非接触)。`.steering`+auto-memory は
2 拠点非同期ゆえ handoff は repo-tracked spec + committed golden に焼く。**設計 FROZEN。**

## Scope
### In
- `integration/embodied/handoff.py` 新規:
  - converter: `TraceRow`→`ControlEnvelope` (MoveMsg/AnimationMsg/SpeechMsg) の envelope_stream 生成
  - `manifest.json` builder: SCHEMA_VERSION + {run_id,seed,agent_ids,world_tick_count} + env pins +
    coordinate convention (Y-up/XZ 接地/meters/yaw=atan2(dz,dx)) + tick mapping (physics_tick_index vs
    agent_tick) + determinism checklist + canonical JSON rules + artifact hashes + Godot headless command
  - committed golden 生成 (1 agent × 8 tick、grill G-5): manifest+ecl_trace+decisions+期待 checksum
- `godot_project/scripts/dev/EclReplayPlayer.gd` 新規 (dev-only、FixturePlayer と別、本番 WS 非接触):
  manifest→envelope_stream を order_slot 順に offline 再生
- `tests/fixtures/ecl_v0_golden/{manifest.json, ecl_trace.jsonl, decisions.jsonl}` (committed)
- `scripts/repro.sh` 規約の 1 コマンド replay 経路
### Out
- full-session 可視化 (Milestone 4)
- 本番 Godot WS 経路への接続
- measurement 出力 (holding)

## Allowed Files
- `src/erre_sandbox/integration/embodied/handoff.py` (新規)
- `godot_project/scripts/dev/EclReplayPlayer.gd` (新規)
- `tests/test_integration/test_ecl_handoff.py` (新規)
- `tests/fixtures/ecl_v0_golden/**` (committed golden)
- `scripts/` (repro 経路、必要なら)

## Acceptance Criteria (AC↔test マッピング)
- AC1 manifest pin: `test_ecl_handoff.py::test_ecl_v0_handoff_manifest_pins` 緑
  — SCHEMA_VERSION / coordinate convention / physics_tick_index vs agent_tick 二軸 / determinism
    checklist を assert
- AC2 golden 一致: `test_ecl_handoff.py::test_ecl_v0_handoff_golden_sample_matches` 緑
  — committed golden の trace/decisions を replay し期待 checksum に byte 一致 (cross-machine 再現契約)
- AC3 converter schema: `test_ecl_handoff.py::test_ecl_v0_envelope_conformance` 緑
  — converter 出力が ControlEnvelope schema に conformant、order_slot 順序が決定的
- AC4 Godot smoke: `EclReplayPlayer.gd` が manifest+envelope_stream を parse し order_slot 順に
  再生 (headless smoke、dev-only、本番 WS 非接触を確認)
- AC5 CI parity: `bash scripts/dev/pre-push-check.sh` 4 段全 pass

## Test Plan
`pytest -q tests/test_integration/test_ecl_handoff.py` + golden replay 1 コマンド再現 (`scripts/repro.sh`
規約) + Godot headless smoke (`godot --headless` で EclReplayPlayer parse/再生、CI 非対象なら手動記録) +
pre-push CI parity 4 段。

## Stop Conditions
- 全 AC 緑 (Done)
- Godot 側 replay 契約が現行 FixturePlayer と衝突し dev-only 分離が破れる (Stop、bounded scope 逸脱)
- committed golden が非決定 (checksum drift) (Stop、Plane pin の穴、I4 に戻す)
- budget 到達 (Stop)

## Dependencies
- **I1〜I4 完了必須** (loop harness の trace/decisions/checksum を消費)
- 参照 (read-only): `godot_project/scripts/dev/FixturePlayer.gd` (dev harness 前例、DEFAULT_PLAYLIST 固定)
- import: `integration.embodied.loop` / `schemas.ControlEnvelope` 系

## Status
DONE (in-session 逐次、feat/ecl-v0)

## Execution Result
- **新規ファイル (Allowed Files のみ、loop/cycle/world/embodiment 無改変を git diff --stat で証明)**:
  - `src/erre_sandbox/integration/embodied/handoff.py` — 純モジュール (httpx 非依存)。converter
    (`build_envelope_stream`: decisions→ordered ControlEnvelope stream)、manifest builder
    (SCHEMA_VERSION + coord convention Y-up/XZ/meters/yaw=atan2 + 二軸 tick mapping + determinism
    checklist + canonical JSON rules + artifact SHA-256 + replay_checksum)、jsonl serializers/loaders、
    golden 定数 (agent_state/persona/recorded_calls/run params)、`render_golden`/`write_golden`。
  - `scripts/ecl_v0_golden.py` — offline bake/verify (mock embedding + replay-mode run_ecl_loop、Ollama 非依存)。
  - `scripts/repro.sh` — 1 コマンド offline replay verify (reproducibility-discipline)。
  - `godot_project/scripts/dev/EclReplayPlayer.gd` — dev-only、SceneTree standalone、manifest+
    envelope_stream を (order_slot,agent_tick,seq) 順に offline 再生。FixturePlayer と別・本番 WS 非接触。
  - `tests/test_integration/test_ecl_handoff.py` — AC1/AC2/AC3 + measurement 非再入 import/output guard。
  - `tests/fixtures/ecl_v0_golden/{manifest.json, ecl_trace.jsonl(160), decisions.jsonl(8),
    envelope_stream.jsonl(24)}` — committed golden。replay_checksum =
    `5a25b825eaa5d5b20de2251d77d8eba525f4a7aff2e4a3a2d5cb46edc047a60e`。
- **AC 結果**:
  - AC1 (manifest pins): `test_ecl_v0_handoff_manifest_pins` 緑。
  - AC2 (golden 一致): `test_ecl_v0_handoff_golden_sample_matches` 緑 (committed decisions のみで replay →
    ecl_trace_checksum byte 一致)。`bash scripts/repro.sh` = GOLDEN OK。
  - AC3 (converter schema): `test_ecl_v0_envelope_conformance` 緑 (24 envelope schema-conformant +
    (order_slot,agent_tick,seq) 決定順)。
  - AC4 (Godot smoke): **G-GEAR に Godot 未 install ゆえ headless 実行は MacBook 側手動工程に defer**
    (CI 非対象、`GODOT_HEADLESS_COMMAND` を manifest に焼き込み)。.gd は godot-gdscript Skill 準拠で
    静的レビュー済 (SceneTree standalone / kind 分岐 / 本番 WS 非接触 / dev/ 分離)。
  - AC5 (CI parity): ruff format --check / ruff check / mypy src 全 pass。pytest 全緑 (実行中→確定後追記)。
- **実装中発見**: decisions.jsonl は wall-clock provenance (`agent_state.wall_clock` /
  `reasoning_trace.created_at`、frozen I3/I4 の `_utc_now` 既定・sent_at pin 対象外) を含み byte 非再現。
  権威的再現性 = `ecl_trace_checksum` (trace 160 rows) は drift なし。envelope_stream は agent_update/
  reasoning_trace を除外ゆえ byte 決定的。詳細 = decisions.md 判断 G-11。Stop 非該当 (checksum drift なし)。
