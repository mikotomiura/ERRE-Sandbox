# Issue 004: integration/embodied/loop.py determinism harness + replay checksum + reflection disable
verify_level: recheck   # 決定性契約 (byte 再現) + two-plane 記録閉集合の主成果

## Goal
I3 が公開した ECL live seam を **integration 層のみで** 駆動する純 record/replay harness を
`integration/embodied/loop.py` に実装する (Codex TASK-PRE: cycle/world は無改変、seam は I3 所有)。
EclRecordMode を構築 (factories/flags 注入) して cycle+world を駆動、全 LLM call を記録し replay で固定
(Plane 2)、trace row を physics sink closure + CycleResult decision provenance から assemble、同一 seed +
記録済 Plane2 で trace_checksum が byte 再現することを実証する。reflection が record mode で発火しないことを
end-to-end で pin する。

## Background
design-final §論点3 (two-plane determinism 閉集合) + DA-ECLIMPL-4 + Codex HIGH-1 (reflection)/
M2 (tick 二軸)/M3 (order_slot)。**Codex TASK-PRE HIGH-1 反映: cycle/world seam は I3 が既に公開済ゆえ I4 は
integration のみ**。integration は全層 import 可。ecl_trace_sink closure が run_id を bind し EclTraceRow を
組み立てる (house-style、world は integration 非 import)。frozen `running/policy.py` の named substream /
SmokeClock 相当 fixed-dt clock / trace_checksum 設計を写経。**設計 FROZEN。**

## Scope
### In (integration/embodied/ のみ、cycle/world 無改変)
- `integration/embodied/__init__.py` + `loop.py` 新規:
  - `EclRecordMode` を構築し CognitionCycle(ecl_mode=...) + WorldRuntime(ecl_trace_sink=closure) を
    construction-mode で駆動 (fixed-dt clock 注入 / named RNG substream `ecl-{run_id}-{agent_id}-{stream}` /
    deterministic id·ts·now factory を I3 の注入点へ供給)
  - `ecl_trace_sink` closure: I3 が渡す primitive (agent_id, physics_tick_index, x,y,z,yaw,pitch,zone) に
    run_id を bind し `EclTraceRow` (integration-local 型) を組み立て。move-decision provenance
    (resolved_from/centroid_provenance/jitter/pre_clamp/post_clamp) は CycleResult から agent_tick 軸で assemble
  - Plane 2 記録: 発火した全 action LLM call の post-processed full LLMPlan + fallback/parser/schema
    version + bias resample + envelope provenance + raw response + prompt + sampling
  - replay: 記録済 LLM 出力を注入し LLM を呼ばない
  - `trace_checksum` (SHA-256、frozen 写経)
### Out
- cycle.py / world/tick.py の改変 (I3 が seam を所有・公開済)
- converter/manifest/golden/Godot (I5)
- multi-agent 実体化 (log slot は前方互換で入れるが単一 agent、side effects は disable/stub)
- 統計/floor/landscape 出力 (holding)

## Allowed Files
- `src/erre_sandbox/integration/embodied/__init__.py` (新規)
- `src/erre_sandbox/integration/embodied/loop.py` (新規)
- `tests/test_integration/test_ecl_loop.py` (新規)

## Acceptance Criteria (AC↔test マッピング)
- AC1 replay byte 再現: `test_ecl_loop.py::test_ecl_v0_replay_checksum_stable` 緑
  — 同一 seed + 記録済 Plane2 で 2 回 run し trace_checksum が byte 一致
- AC2 reflection disable: `test_ecl_loop.py::test_ecl_v0_reflection_disabled_in_record_mode` 緑
  — ECL record mode で reflection LLM が発火しない (Reflector.maybe_reflect 非呼び出し or no-op)
- AC3 log 前方互換: `test_ecl_loop.py::test_ecl_v0_log_schema_forward_compat` 緑
  — trace 行に agent_id/physics_tick_index/agent_tick/order_slot が存在し order_slot が
    `sorted(agent_id)` 決定性 (単一 agent でも slot 決定的)
- AC4 Plane2 記録完全性: `test_ecl_loop.py::test_ecl_v0_plane2_full_llm_record` 緑
  — 記録済 decisions に full LLMPlan + fallback/parser/prompt/sampling が揃い、replay がそれのみで
    state を再構成 (fresh LLM を呼ばない)
- AC5 CI parity: `bash scripts/dev/pre-push-check.sh` 4 段全 pass

## Test Plan
`pytest -q tests/test_integration/test_ecl_loop.py` (test-standards: LLM は記録済 LLMPlan replay 注入で
モック、実 Ollama 非依存、sqlite-vec `:memory:`) + pre-push CI parity 4 段。checksum は 2 回 run diff。

## Stop Conditions
- 全 AC 緑 (Done)
- Plane 1 に未 pin の非決定源が判明 (Stop、設計に列挙されていない source は superseding ADR)
- budget 到達 (Stop)

## Dependencies
- **I1 + I2 + I3 完了必須** (I3 が cycle/world seam を全公開 — ecl_mode 注入点 / ecl_trace_sink /
  CycleResult decision provenance / reflection skip。I4 は seam を無改変で駆動するのみ)
- 写経元 (read-only): `evidence/d0_substrate/running/policy.py` (named substream/`_transit`/SmokeClock) +
  `stub.py` (`trace_checksum`/`TraceRow`) + `evidence/d0_substrate/smoke.SmokeClock`
- import: `cognition.cycle` / `world.tick` / `world.physics` / `memory.*` / `schemas`

## Status
QUEUED

## Execution Result
(完了時記入)
