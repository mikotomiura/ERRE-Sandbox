# Issue 001: contracts/geometry.py 幾何 SSOT 昇格 + world/zones.py re-export shim
verify_level: recheck   # 全下流が依存する contract + 公開 import 面変更ゆえ独立再検証

## Goal
ECL 器官の全層が同一幾何定数を合法参照できるよう、`world/zones.py` の幾何 SSOT を
`contracts/geometry.py` へ昇格し、`world/zones.py` を re-export shim 化する。既存 import を
1 つも破壊せず、layer import guard が緑になるまで完結する。frozen `running/policy.py` の
`disc_jitter` / `reflect_clamp` は contracts/geometry.py に新規写経する。

## Background
design-final §論点1 + DA-ECLIMPL-1 + Codex LOW-1。cognition は world を import できない
(architecture-rules)。履歴依存 resolution を cognition で行うには幾何定数を contracts
(schemas/pydantic/stdlib のみ依存) に置く必要がある。M2_THRESHOLDS/contracts/thresholds.py が
同型の昇格+shim 前例。**設計 FROZEN、再オープンしない。**

## Scope
### In
- `contracts/geometry.py` 新規: `WORLD_SIZE_M` / `ZONE_CENTERS` / `locate_zone` / `default_spawn`
  を world/zones.py から移動 + `CELL_MICRO_RADIUS_M`(=10.0) / `reflect_clamp` / `disc_jitter` を
  frozen `running/policy.py` L214-241 から新規写経 (import せず設計コピー)
- `world/zones.py`: 上記 4 定数/関数を contracts/geometry から re-export する shim 化
  (`ZONE_PROPS` / `ADJACENCY` / `PropSpec` / `adjacent_zones` は据え置き)
### Out
- `ZONE_PROPS` / `ADJACENCY` の移動 (Godot sync / world walk 用ゆえ zones.py 据え置き、Codex L1)
- embodiment resolver 本体 (I2)
- frozen `evidence/**` 変更 (read-only、写経のみ)

## Allowed Files
- `src/erre_sandbox/contracts/geometry.py` (新規)
- `src/erre_sandbox/world/zones.py` (shim 化)
- `tests/test_contracts/test_geometry.py` (新規)
- `tests/test_world/test_zones.py` (既存があれば shim 不変性追記)

## Acceptance Criteria (AC↔test マッピング)
- AC1 SSOT 一致: `test_geometry.py::test_geometry_matches_legacy_zones` 緑
  — contracts/geometry の `WORLD_SIZE_M`/`ZONE_CENTERS`/`locate_zone`/`default_spawn` が旧
  world/zones の値と byte 一致 (shim re-export 経由でも同一 object/値)
- AC2 shim 後方互換: `test_geometry.py::test_zones_reexport_backward_compat` 緑
  — `from erre_sandbox.world.zones import WORLD_SIZE_M, ZONE_CENTERS, locate_zone, default_spawn`
  が引き続き成功し contracts と同値
- AC3 幾何写経: `test_geometry.py::test_disc_jitter_and_reflect_clamp` 緑
  — `disc_jitter` の r=R√u 分布性 (半径 ≤ CELL_MICRO_RADIUS_M) + `reflect_clamp` が cell 外 dest を
  ρ_max 内へ clamp し `locate_zone(clamped)==Z`、cell 内 dest は不変 (frozen 挙動と等価)
- AC4 layer import guard (片方向、循環なし): `test_geometry.py::test_contracts_geometry_no_upper_import` 緑
  — contracts/geometry が world/cognition/memory/inference/ui/integration/evidence を import しない
  (schemas/pydantic/stdlib のみ)。依存は **`world.zones → contracts.geometry` の片方向固定**
  (Codex TASK-PRE MEDIUM: contracts.geometry が world.zones を import した瞬間に循環破綻)
- AC5 cold import 非循環: `test_geometry.py::test_cold_import_no_cycle` 緑
  — `contracts.geometry` / `world.zones` / `world.physics` / `world.__init__` を**個別に** import しても
  循環せず成功 (Codex TASK-PRE MEDIUM)
- AC6 CI parity: `bash scripts/dev/pre-push-check.sh` 4 段 (ruff format --check / ruff check /
  mypy src / pytest -q) 全 pass、末尾 `ALL CHECKS PASSED`

## Test Plan
`pytest -q tests/test_contracts/test_geometry.py tests/test_world/` + 既存全 test 回帰
(import 面変更が既存 world/evidence import を壊さないこと) + pre-push CI parity 4 段。

## Stop Conditions
- 全 AC 緑 (Done)
- contracts 昇格が他 ADR/循環 import と衝突 → option-c 退避判断が要る (Stop、escalate、DA-ECLIMPL-1
  の door を人が開ける)
- budget 到達 (Stop)

## Dependencies
- なし (foundation slice、I2-I5 の前提)
- 写経元 (read-only): `evidence/d0_substrate/running/policy.py` L214-241 (`_disc_jitter`/`_reflect_clamp`)
- 前例: `contracts/thresholds.py` (昇格+shim), `world/zones.py` (現行)

## Status
DONE (2026-07-05、in-session feat/ecl-v0)。AC1〜AC6 全緑、pre-push CI parity 4 段 ALL CHECKS PASSED
(3323 passed, 66 skipped)。

## Execution Result
`contracts/geometry.py` 新規 (WORLD_SIZE_M/ZONE_CENTERS/locate_zone/default_spawn 移動 +
CELL_MICRO_RADIUS_M=10.0/disc_jitter/reflect_clamp を frozen policy.py 写経) + `world/zones.py` を
re-export shim 化 (ZONE_PROPS/ADJACENCY/PropSpec/adjacent_zones 据え置き、_ZONE_OFFSET を contracts から
import) + `tests/test_contracts/test_geometry.py` (14 tests: relocated values / shim is-identity /
disc_jitter within-radius / reflect_clamp clamp 両分岐 / 片方向 import guard / cold import 非循環)。
既存 test_zones.py + d0 mirror pin (test_d0_stub) 無改変 pass = 値 byte 不変を確認。
Codex TASK-PRE MEDIUM (循環 import 片方向 + cold import test) 反映済。
