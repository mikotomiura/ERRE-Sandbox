{
  "$defs": {
    "BehaviorProfile": {
      "additionalProperties": false,
      "description": "Per-persona observable behavior knobs (M7ζ-3, additive).\n\nDrives runtime divergence between agents (movement speed, cognition tick\ncadence, post-MoveMsg dwell, separation radius) so that the same\ncognition cycle code can produce visibly different \"creatures\" in live\nobservation. Server-side only — never flows over the wire, so\n``SCHEMA_VERSION`` is not bumped when fields are added here.",
      "properties": {
        "cognition_period_s": {
          "default": 10.0,
          "description": "Base period of this agent's cognition tick. The 10 s global scheduler in ``world/tick.py`` runs unchanged, but each agent's ``next_cognition_due`` is advanced by this value (phase wheel) so Nietzsche-like bursts and Rikyū-like slow cadences emerge.",
          "maximum": 120.0,
          "minimum": 3.0,
          "title": "Cognition Period S",
          "type": "number"
        },
        "dwell_time_s": {
          "default": 0.0,
          "description": "Extra delay added once after a MoveMsg fires (seiza-like dwell). Suppresses cognition ticks for this duration before the phase wheel resumes — Rikyū's ≥20 min seiza is the headline use case.",
          "maximum": 600.0,
          "minimum": 0.0,
          "title": "Dwell Time S",
          "type": "number"
        },
        "movement_speed_factor": {
          "default": 1.0,
          "description": "Multiplier applied to ``CognitionCycle.DEFAULT_DESTINATION_SPEED`` (1.3 m/s) when emitting MoveMsg. Yields the persona's distinctive gait rhythm in the live ``MoveMsg.speed`` histogram.",
          "maximum": 2.5,
          "minimum": 0.3,
          "title": "Movement Speed Factor",
          "type": "number"
        },
        "separation_radius_m": {
          "default": 1.5,
          "description": "Pair distance (XZ plane) below which the backend nudges this agent and its peer apart by 0.4 m per physics tick. Stays well inside ``_PROXIMITY_THRESHOLD_M = 5.0`` so dialog scheduler proximity events keep firing.",
          "maximum": 5.0,
          "minimum": 0.0,
          "title": "Separation Radius M",
          "type": "number"
        }
      },
      "title": "BehaviorProfile",
      "type": "object"
    },
    "CognitiveHabit": {
      "additionalProperties": false,
      "description": "One recurring cognitive-behavioural pattern of a historical figure.",
      "properties": {
        "description": {
          "description": "Concrete habit, present tense.",
          "title": "Description",
          "type": "string"
        },
        "flag": {
          "$ref": "#/$defs/HabitFlag",
          "description": "fact / legend / speculative."
        },
        "mechanism": {
          "description": "Cognitive-neuroscience mechanism hypothesised to underlie it.",
          "title": "Mechanism",
          "type": "string"
        },
        "source": {
          "description": "Citation key, e.g. 'kuehn2001'.",
          "title": "Source",
          "type": "string"
        },
        "trigger_zone": {
          "anyOf": [
            {
              "$ref": "#/$defs/Zone"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Zone that typically activates this habit, if any."
        }
      },
      "required": [
        "description",
        "source",
        "flag",
        "mechanism"
      ],
      "title": "CognitiveHabit",
      "type": "object"
    },
    "HabitFlag": {
      "description": "Epistemic status of a cognitive habit attributed to a historical figure.",
      "enum": [
        "fact",
        "legend",
        "speculative"
      ],
      "title": "HabitFlag",
      "type": "string"
    },
    "PersonalityTraits": {
      "additionalProperties": false,
      "description": "Static personality: Big Five + ERRE-specific Japanese aesthetic traits.",
      "properties": {
        "agreeableness": {
          "default": 0.5,
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Agreeableness",
          "type": "number"
        },
        "conscientiousness": {
          "default": 0.5,
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Conscientiousness",
          "type": "number"
        },
        "extraversion": {
          "default": 0.5,
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Extraversion",
          "type": "number"
        },
        "ma_sense": {
          "default": 0.5,
          "description": "Tolerance of silence.",
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Ma Sense",
          "type": "number"
        },
        "neuroticism": {
          "default": 0.5,
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Neuroticism",
          "type": "number"
        },
        "openness": {
          "default": 0.5,
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Openness",
          "type": "number"
        },
        "wabi": {
          "default": 0.5,
          "description": "Acceptance of imperfection.",
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Wabi",
          "type": "number"
        }
      },
      "title": "PersonalityTraits",
      "type": "object"
    },
    "SamplingBase": {
      "additionalProperties": false,
      "description": "Absolute sampling parameters used as the persona's baseline.",
      "properties": {
        "repeat_penalty": {
          "default": 1.0,
          "maximum": 2.0,
          "minimum": 0.5,
          "title": "Repeat Penalty",
          "type": "number"
        },
        "temperature": {
          "default": 0.7,
          "maximum": 2.0,
          "minimum": 0.0,
          "title": "Temperature",
          "type": "number"
        },
        "top_p": {
          "default": 0.9,
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Top P",
          "type": "number"
        }
      },
      "title": "SamplingBase",
      "type": "object"
    },
    "Zone": {
      "description": "Five spatial zones of the world (see ``docs/glossary.md``).",
      "enum": [
        "study",
        "peripatos",
        "chashitsu",
        "agora",
        "garden"
      ],
      "title": "Zone",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Root schema for a persona YAML (one file per historical figure).",
  "properties": {
    "behavior_profile": {
      "$ref": "#/$defs/BehaviorProfile"
    },
    "cognitive_habits": {
      "items": {
        "$ref": "#/$defs/CognitiveHabit"
      },
      "title": "Cognitive Habits",
      "type": "array"
    },
    "default_sampling": {
      "$ref": "#/$defs/SamplingBase"
    },
    "display_name": {
      "title": "Display Name",
      "type": "string"
    },
    "era": {
      "description": "Dates, e.g. '1724-1804'.",
      "title": "Era",
      "type": "string"
    },
    "persona_id": {
      "description": "kebab-case id, e.g. 'kant'.",
      "title": "Persona Id",
      "type": "string"
    },
    "personality": {
      "$ref": "#/$defs/PersonalityTraits"
    },
    "preferred_zones": {
      "items": {
        "$ref": "#/$defs/Zone"
      },
      "title": "Preferred Zones",
      "type": "array"
    },
    "primary_corpus_refs": {
      "items": {
        "type": "string"
      },
      "title": "Primary Corpus Refs",
      "type": "array"
    },
    "schema_version": {
      "default": "0.10.0-m7h",
      "title": "Schema Version",
      "type": "string"
    }
  },
  "required": [
    "persona_id",
    "display_name",
    "era",
    "personality",
    "cognitive_habits",
    "preferred_zones"
  ],
  "title": "PersonaSpec",
  "type": "object"
}
